import{_ as n,C as i,o as s,c as d,j as a,a as t,b as l,w as r,E as p,a3 as c,a2 as h}from"./chunks/framework.CZqXuAiL.js";const w=JSON.parse('{"title":"Architecture Overview — Apple","description":"How the Apple admin theme works — Blueprint injection points, the runtime nav harvest, the token layer, the dashboard patch, settings storage, and the dual Blueprint/standalone packaging.","frontmatter":{"title":"Architecture Overview — Apple","description":"How the Apple admin theme works — Blueprint injection points, the runtime nav harvest, the token layer, the dashboard patch, settings storage, and the dual Blueprint/standalone packaging."},"headers":[],"relativePath":"apple/architecture/overview.md","filePath":"apple/architecture/overview.md","lastUpdated":null}'),b={name:"apple/architecture/overview.md"};function u(m,e,v,g,f,k){const o=i("Mermaid");return s(),d("div",null,[e[1]||(e[1]=a("h1",{id:"architecture-overview",tabindex:"-1"},[t("Architecture Overview "),a("a",{class:"header-anchor",href:"#architecture-overview","aria-label":'Permalink to "Architecture Overview"'},"​")],-1)),e[2]||(e[2]=a("p",null,[t("Apple is a Blueprint extension that themes the admin area through "),a("strong",null,"runtime injection only"),t(". No core file is replaced for the theme to work; the one deliberate exception (the dashboard view) is a reversible, marker-verified patch.")],-1)),e[3]||(e[3]=a("h2",{id:"system-diagram",tabindex:"-1"},[t("System diagram "),a("a",{class:"header-anchor",href:"#system-diagram","aria-label":'Permalink to "System diagram"'},"​")],-1)),(s(),l(c,null,{default:r(()=>[p(o,{id:"mermaid-9",class:"mermaid",graph:"flowchart%20TD%0A%20%20%20%20subgraph%20Install%5B%22blueprint%20-i%20apple%22%5D%0A%20%20%20%20%20%20%20%20Z%5B%22apple-v1.0.0.blueprint%22%5D%0A%20%20%20%20%20%20%20%20Z%20--%3E%20P1%5B%22Hub%20controller%20%2B%20view%3Cbr%2F%3EAdmin%20%E2%86%92%20Apple%20Theme%22%5D%0A%20%20%20%20%20%20%20%20Z%20--%3E%20P2%5B%22public%2F%20%E2%86%92%20public%2Fextensions%2Fapple%3Cbr%2F%3E5%20CSS%20libs%20%2B%20sidebar.js%22%5D%0A%20%20%20%20%20%20%20%20Z%20--%3E%20P3%5B%22admin%20wrapper%20%E2%86%92%20Blueprint's%3Cbr%2F%3Eevery-admin-page%20render%20hook%22%5D%0A%20%20%20%20%20%20%20%20Z%20--%3E%20P4%5B%22src%2Fviews%20%E2%86%92%20variables%20%2B%20dashboard%20partials%22%5D%0A%20%20%20%20%20%20%20%20Z%20--%3E%20P5%5B%22private%2Finstall.sh%3Cbr%2F%3Epatches%20admin%2Findex.blade.php%3Cbr%2F%3E(backup%20%2B%20marker%20verify)%22%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20Request%5B%22Every%20admin%20page%20request%22%5D%0A%20%20%20%20%20%20%20%20L%5B%22layouts%2Fadmin.blade.php%3Cbr%2F%3E%40yield('blueprint.wrappers')%22%5D%20--%3E%20W%5B%22apple%20wrapper%22%5D%0A%20%20%20%20%20%20%20%20W%20--%3E%20S%5B%22read%20apple%3A%3A*%20settings%3Cbr%2F%3E(Blueprint%20library%2C%20unserialized)%22%5D%0A%20%20%20%20%20%20%20%20S%20--%3E%20V%5B%22variables.blade.php%3Cbr%2F%3Eemits%20--ap-*%20tokens%20for%20both%20modes%22%5D%0A%20%20%20%20%20%20%20%20V%20--%3E%20PP%5B%22pre-paint%20script%3Cbr%2F%3ElocalStorage%20%E2%86%92%20data-apple%2C%20rail%2C%20no-motion%22%5D%0A%20%20%20%20%20%20%20%20PP%20--%3E%20LD%5B%22%23ap-loader%20splash%22%5D%0A%20%20%20%20%20%20%20%20LD%20--%3E%20C%5B%22core%20%C2%B7%20layout%20%C2%B7%20components%20%C2%B7%3Cbr%2F%3Edashboard%20%C2%B7%20mobile%20CSS%22%5D%0A%20%20%20%20%20%20%20%20LD%20--%3E%20J%5B%22sidebar.js%22%5D%0A%20%20%20%20%20%20%20%20J%20--%3E%20HV%5B%22harvest%20ul.sidebar-menu%22%5D%0A%20%20%20%20%20%20%20%20HV%20--%3E%20CH%5B%22build%20%23ap-sidebar%20%2B%20%23ap-topbar%3Cbr%2F%3Emove%20stock%20action%20links%22%5D%0A%20%20%20%20%20%20%20%20CH%20--%3E%20B%5B%22.ap-booted%20%E2%86%92%20loader%20fades%22%5D%0A%20%20%20%20end%0A"})]),fallback:r(()=>[...e[0]||(e[0]=[t(" Loading... ",-1)])]),_:1})),e[4]||(e[4]=h(`<h2 id="injection-points-conf-yml" tabindex="-1">Injection points (conf.yml) <a class="header-anchor" href="#injection-points-conf-yml" aria-label="Permalink to &quot;Injection points (conf.yml)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Point</th><th>Value</th><th>What it does</th></tr></thead><tbody><tr><td><code>admin.wrapper</code></td><td><code>admin/wrapper.blade.php</code></td><td>The theme&#39;s <strong>only</strong> entry point — rendered by Blueprint at the end of <code>&lt;body&gt;</code> on every admin page. Emits tokens, loader, asset links, and the settings-link injector.</td></tr><tr><td><code>admin.controller</code> / <code>admin.view</code></td><td>hub</td><td>Settings UI; SCHEMA-driven; seeds defaults on load.</td></tr><tr><td><code>data.public</code></td><td><code>public/</code></td><td>Static assets served from <code>/extensions/apple</code> with <code>{timestamp}</code> cache-busting.</td></tr><tr><td><code>data.directory</code></td><td><code>private/</code></td><td><code>install.sh</code> / <code>remove.sh</code> hooks.</td></tr><tr><td><code>requests.views</code></td><td><code>src/views/</code></td><td><code>variables</code> (token emission) and <code>dashboard</code> (bento source) partials.</td></tr></tbody></table><h2 id="the-token-layer" tabindex="-1">The token layer <a class="header-anchor" href="#the-token-layer" aria-label="Permalink to &quot;The token layer&quot;">​</a></h2><p><code>variables.blade.php</code> emits one inline <code>&lt;style&gt;</code> with the full Catppuccin palette <strong>for both modes</strong> — <code>html[data-apple=&quot;dark&quot;]</code> gets Mocha values, <code>html[data-apple=&quot;light&quot;]</code> gets Latte — plus the admin&#39;s accent (validated against a server-side whitelist), glass and radius settings as <code>--ap-*</code> custom properties. Every stylesheet consumes variables only; there are no hardcoded colors anywhere in the libraries.</p><p>A synchronous pre-paint script resolves the effective mode (browser choice → hub default → OS preference) and sets <code>data-apple</code> on <code>&lt;html&gt;</code> before first paint, so there is no mode flash.</p><h2 id="the-runtime-harvest" tabindex="-1">The runtime harvest <a class="header-anchor" href="#the-runtime-harvest" aria-label="Permalink to &quot;The runtime harvest&quot;">​</a></h2><p><code>sidebar.js</code> reads the stock <code>ul.sidebar-menu</code> — headers, links, icon classes, labels, active state — and builds <code>#ap-sidebar</code> and <code>#ap-topbar</code>. The stock chrome stays in the DOM (AdminLTE JS and extensions may depend on it) but is hidden. Stock top-bar actions are <strong>re-parented</strong> into the Apple top bar rather than cloned, so panel-bound handlers (logout confirmation, tooltips) keep working. See <a href="./../user-guide/compatibility">Extension Compatibility</a>.</p><h2 id="the-dashboard-patch" tabindex="-1">The dashboard patch <a class="header-anchor" href="#the-dashboard-patch" aria-label="Permalink to &quot;The dashboard patch&quot;">​</a></h2><p><code>install.sh</code> swaps <code>resources/views/admin/index.blade.php</code> for Apple&#39;s bento view:</p><ol><li>Skip if the marker <code>apple-theme-dashboard</code> is already present (idempotent).</li><li>Back up the original to <code>index.blade.php.apple-backup</code> (first run only).</li><li>Copy the bento source — probed from three locations because the hook can run before Blueprint links the views.</li><li>Verify the marker landed; roll back to the backup if not.</li></ol><p>The patched view reads <code>apple::dashboard_custom</code> at render time and falls back to the <strong>stock markup embedded in the same file</strong> when the feature is off — toggling never requires a re-patch. <code>remove.sh</code> restores the backup.</p><h2 id="settings-storage" tabindex="-1">Settings storage <a class="header-anchor" href="#settings-storage" aria-label="Permalink to &quot;Settings storage&quot;">​</a></h2><p>Settings live as <code>apple::&lt;key&gt;</code> rows in the panel <code>settings</code> table via Blueprint&#39;s library — no migrations, no config files. Two subtleties the code handles explicitly:</p><ul><li><strong>Serialization.</strong> Blueprint&#39;s <code>dbSet</code> stores serialized scalars (<code>s:1:&quot;1&quot;;</code>). Every direct table read (dashboard view, standalone wrapper) unwraps <code>s:N:&quot;…&quot;</code> before comparing.</li><li><strong>Caching.</strong> The library caches reads; after external row edits, <code>php artisan cache:clear</code> is required (the hub re-seeds from whatever it reads, so a stale cache can resurrect deleted values).</li></ul><h2 id="css-libraries" tabindex="-1">CSS libraries <a class="header-anchor" href="#css-libraries" aria-label="Permalink to &quot;CSS libraries&quot;">​</a></h2><table tabindex="0"><thead><tr><th>File</th><th>Responsibility</th></tr></thead><tbody><tr><td><code>core.css</code></td><td>Surface/typography, boot gate + loader, scrollbars, selection, focus ring, motion killswitches</td></tr><tr><td><code>layout.css</code></td><td>Sidebar, top bar, content flow, iOS large-title header, footer, rail mode</td></tr><tr><td><code>components.css</code></td><td>The global AdminLTE/Bootstrap 3 reskin (see Compatibility)</td></tr><tr><td><code>dashboard.css</code></td><td>The bento grid and its tiles, responsive collapse</td></tr><tr><td><code>mobile.css</code></td><td>≤ 992px drawer behavior, ≤ 768px content adjustments</td></tr></tbody></table><p>All selectors are scoped under <code>html[data-apple]</code>, so any page where the theme is inactive renders 100% stock.</p><h2 id="standalone-variant" tabindex="-1">Standalone variant <a class="header-anchor" href="#standalone-variant" aria-label="Permalink to &quot;Standalone variant&quot;">​</a></h2><p>For panels without Blueprint, the same theme ships as PanelFiles + scripts:</p><ul><li>Assets land in <code>public/apple</code>, partials in <code>resources/views/apple</code>.</li><li><code>install.sh</code> injects a marker-delimited <code>@include(&#39;apple.wrapper&#39;)</code> before <code>&lt;/body&gt;</code> in <code>layouts/admin.blade.php</code> (backup first, idempotent) and applies the same dashboard patch.</li><li>The standalone wrapper is identical except it reads settings straight from the table (with unserialization) and links assets from <code>/apple</code> — no Blueprint dependency.</li></ul><h2 id="file-layout" tabindex="-1">File layout <a class="header-anchor" href="#file-layout" aria-label="Permalink to &quot;File layout&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>apple/</span></span>
<span class="line"><span>├── conf.yml                    # Blueprint manifest</span></span>
<span class="line"><span>├── icon.svg</span></span>
<span class="line"><span>├── admin/</span></span>
<span class="line"><span>│   ├── controller.php          # settings hub (SCHEMA-driven)</span></span>
<span class="line"><span>│   ├── view.blade.php          # settings UI</span></span>
<span class="line"><span>│   └── wrapper.blade.php       # every-page injector</span></span>
<span class="line"><span>├── src/views/</span></span>
<span class="line"><span>│   ├── variables.blade.php     # --ap-* token emission + pre-paint script</span></span>
<span class="line"><span>│   └── dashboard.blade.php     # bento source (stock fallback embedded)</span></span>
<span class="line"><span>├── public/libraries/           # 5 CSS libs + sidebar.js</span></span>
<span class="line"><span>├── private/</span></span>
<span class="line"><span>│   ├── install.sh              # dashboard patch (idempotent, verified)</span></span>
<span class="line"><span>│   └── remove.sh               # restore + settings cleanup (never fails)</span></span>
<span class="line"><span>└── standalone/</span></span>
<span class="line"><span>    ├── PanelFiles/             # public/apple + resources/views/apple</span></span>
<span class="line"><span>    └── data/                   # install.sh / remove.sh</span></span></code></pre></div>`,22))])}const B=n(b,[["render",u]]);export{w as __pageData,B as default};
