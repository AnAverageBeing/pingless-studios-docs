import{_ as s,o as a,c as i,a2 as t}from"./chunks/framework.CZqXuAiL.js";const k=JSON.parse('{"title":"Installation","description":"Install the Sentinel panel addon (Blueprint or standalone) and the Go node agent on each Wings node, with post-install verification, troubleshooting and uninstall steps.","frontmatter":{"title":"Installation","description":"Install the Sentinel panel addon (Blueprint or standalone) and the Go node agent on each Wings node, with post-install verification, troubleshooting and uninstall steps."},"headers":[],"relativePath":"pterodactyl-sentinel/getting-started/installation.md","filePath":"pterodactyl-sentinel/getting-started/installation.md","lastUpdated":1785525039000}'),n={name:"pterodactyl-sentinel/getting-started/installation.md"};function l(o,e,h,r,d,p){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="installation" tabindex="-1">Installation <a class="header-anchor" href="#installation" aria-label="Permalink to &quot;Installation&quot;">​</a></h1><p>Sentinel has two halves, installed separately: the <strong>panel addon</strong> (once, on the Pterodactyl panel host) and the <strong>node agent</strong> (once per Wings node). Install the panel first — the node installer asks for a token that only exists after the addon is installed.</p><hr><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><h3 id="panel-host" tabindex="-1">Panel host <a class="header-anchor" href="#panel-host" aria-label="Permalink to &quot;Panel host&quot;">​</a></h3><ul><li>Pterodactyl Panel 1.x with PHP 8.2+, working migrations, and admin access.</li><li>Either <a href="https://blueprint.zip" target="_blank" rel="noreferrer">Blueprint</a> installed (for the <code>.blueprint</code> distribution) or plain SSH/root access (for the standalone distribution).</li><li>The panel must be reachable from every Wings node over HTTP(S).</li></ul><h3 id="each-wings-node" tabindex="-1">Each Wings node <a class="header-anchor" href="#each-wings-node" aria-label="Permalink to &quot;Each Wings node&quot;">​</a></h3><ul><li>Linux x86_64 (amd64) or aarch64 (arm64).</li><li>Root access and systemd (<code>systemctl</code> must exist).</li><li>Docker running with the socket at <code>/var/run/docker.sock</code> (standard on Wings nodes).</li><li>Either the Go 1.22+ toolchain (the installer builds from source) or the prebuilt <code>sentinel</code> binary placed next to <code>install.sh</code>.</li><li>Outbound HTTPS from the node to the panel URL.</li></ul><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>The agent runs as root and needs real root: it walks <code>/proc</code> (including other users&#39; processes), talks to the docker socket, and enforces containment (kill, pause, stop, quarantine). The systemd unit intentionally uses light sandboxing.</p></div><hr><h2 id="part-1-—-panel-addon" tabindex="-1">Part 1 — Panel addon <a class="header-anchor" href="#part-1-—-panel-addon" aria-label="Permalink to &quot;Part 1 — Panel addon&quot;">​</a></h2><p>Choose <strong>one</strong> of the two distributions. Both install the same code; the Blueprint package is managed by Blueprint, the standalone installer merges the same PanelFiles tree directly.</p><h3 id="option-a-blueprint" tabindex="-1">Option A: Blueprint <a class="header-anchor" href="#option-a-blueprint" aria-label="Permalink to &quot;Option A: Blueprint&quot;">​</a></h3><ol><li><p>Download <code>Sentinel-vX.Y.Z.zip</code> from the <a href="https://github.com/AnAverageBeing/pterodactyl-sentinel/releases" target="_blank" rel="noreferrer">Releases page</a> and extract it.</p></li><li><p>Copy the <code>.blueprint</code> file to your panel directory:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pterodactylsentinel-vX.Y.Z.blueprint</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/www/pterodactyl/</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/www/pterodactyl</span></span></code></pre></div></li><li><p>Install:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">blueprint</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pterodactylsentinel-vX.Y.Z</span></span></code></pre></div></li></ol><h3 id="option-b-standalone" tabindex="-1">Option B: Standalone <a class="header-anchor" href="#option-b-standalone" aria-label="Permalink to &quot;Option B: Standalone&quot;">​</a></h3><ol><li><p>Extract the release zip and enter the standalone directory:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> standalone/sentinel-standalone</span></span></code></pre></div></li><li><p>Run the installer as root, pointing it at your panel:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> PTERODACTYL_DIRECTORY=/var/www/pterodactyl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install.sh</span></span></code></pre></div><p>The installer is idempotent (safe to re-run), serializes itself against concurrent addon installs with <code>flock /tmp/panel-install.lock</code>, copies the PanelFiles tree into the panel, registers the addon&#39;s routes and sidebar entry, and runs the database migrations. Pass <code>--skip-migrations</code> if you want to run <code>php artisan migrate</code> yourself afterwards.</p></li></ol><h3 id="what-the-panel-install-does" tabindex="-1">What the panel install does <a class="header-anchor" href="#what-the-panel-install-does" aria-label="Permalink to &quot;What the panel install does&quot;">​</a></h3><ul><li>Creates all <code>sentinel_*</code> tables (tokens, events, hashes, flagged servers, scans, quarantines, settings) and seeds default settings.</li><li>Registers <code>routes/api-sentinel.php</code> — the node-facing API at <code>/api/sentinel/node/*</code> with token auth, JSON validation and a 120 req/min throttle.</li><li>Adds the Sentinel section to the admin sidebar with all eight tabs.</li><li>Registers the <code>sentinel:housekeeping</code> artisan command.</li></ul><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>After installing, schedule housekeeping so stale nodes flip offline and old events are pruned. Add it to the panel&#39;s cron entry (the same crontab that runs <code>schedule:run</code>):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> php /var/www/pterodactyl/artisan schedule:run </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /dev/null </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">2&gt;&amp;1</span></span></code></pre></div><p>The command is registered with the scheduler by the addon; you can also run it manually at any time (see <a href="./../user-guide/cli#sentinelhousekeeping">CLI Reference</a>).</p></div><hr><h2 id="part-2-—-node-agent-per-wings-node" tabindex="-1">Part 2 — Node agent (per Wings node) <a class="header-anchor" href="#part-2-—-node-agent-per-wings-node" aria-label="Permalink to &quot;Part 2 — Node agent (per Wings node)&quot;">​</a></h2><h3 id="_1-create-the-pairing-token" tabindex="-1">1. Create the pairing token <a class="header-anchor" href="#_1-create-the-pairing-token" aria-label="Permalink to &quot;1. Create the pairing token&quot;">​</a></h3><p>In the panel admin area, open <strong>Sentinel → Nodes</strong>. Every Pterodactyl node has a card there; click <strong>Create token</strong> (or <strong>View token</strong> if one exists) and copy the 64-hex token.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The panel stores a bcrypt hash for verification plus an encrypted copy so you can re-view the token later. Resetting a token invalidates the old one immediately — the node goes offline until re-paired with the new token.</p></div><h3 id="_2-run-the-installer-on-the-node" tabindex="-1">2. Run the installer on the node <a class="header-anchor" href="#_2-run-the-installer-on-the-node" aria-label="Permalink to &quot;2. Run the installer on the node&quot;">​</a></h3><p>Copy the <code>node-module</code> directory to the Wings node, then as root:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> node-module/install.sh</span></span></code></pre></div><p>The installer is interactive (safe for <code>curl | bash</code> — prompts are read from <code>/dev/tty</code>) and asks for:</p><table tabindex="0"><thead><tr><th>Prompt</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td>Panel URL</td><td>—</td><td>e.g. <code>https://panel.example.com</code>. Trailing slash is stripped. Plain <code>http://</code> prints a cleartext warning.</td></tr><tr><td>Node token</td><td>—</td><td>Exactly 64 hex characters, from Sentinel → Nodes. Case-normalized to lowercase.</td></tr><tr><td>Listen address</td><td><code>0.0.0.0</code></td><td>Address the agent&#39;s API binds to.</td></tr><tr><td>Listen port</td><td><code>8481</code></td><td>Port the panel uses to call back into the node.</td></tr></tbody></table><p>It then:</p><ol><li>Installs the binary to <code>/usr/local/bin/sentinel</code> — built from source if Go is available, otherwise the prebuilt <code>./sentinel</code> (or <code>./build/sentinel-linux-&lt;arch&gt;</code>) next to the script.</li><li>Writes <code>/etc/sentinel/config.yaml</code> and <code>/etc/sentinel/token</code>, both mode <code>0600</code>. Detection settings are <strong>not</strong> in this file — they are panel-managed and pushed to the node; the file only holds node-local settings (API listen, panel URL, token file, state dir, docker socket, log level).</li><li>Installs and starts <code>sentinel-node.service</code> (enabled at boot, <code>Restart=always</code>, 512 MiB memory cap, 100% CPU quota).</li><li>Waits up to 90 seconds for the agent to register with the panel and prints the result.</li></ol><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>The installer is idempotent — re-running it rewrites the config/token and restarts the service. That is the supported way to re-pair a node after a token reset.</p></div><h3 id="_3-firewall-note" tabindex="-1">3. Firewall note <a class="header-anchor" href="#_3-firewall-note" aria-label="Permalink to &quot;3. Firewall note&quot;">​</a></h3><p>The panel calls back into the node&#39;s API (<code>POST /api/v1/config</code>, scan triggers, containment actions, live stats). Allow inbound TCP from the panel host to the agent&#39;s listen port (default <strong>8481</strong>):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ufw example — restrict to the panel&#39;s IP</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ufw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allow</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> from</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">panel-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">p</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> to</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> any</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> port</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8481</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> proto</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tcp</span></span></code></pre></div><p>If you bound the agent to <code>127.0.0.1</code>, config push and panel-initiated actions cannot reach it — heartbeats and event uploads still work, but use a reachable address in production.</p><hr><h2 id="post-install-verification" tabindex="-1">Post-install verification <a class="header-anchor" href="#post-install-verification" aria-label="Permalink to &quot;Post-install verification&quot;">​</a></h2><h3 id="_1-node-health-on-the-node" tabindex="-1">1. Node health (on the node) <a class="header-anchor" href="#_1-node-health-on-the-node" aria-label="Permalink to &quot;1. Node health (on the node)&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://127.0.0.1:8481/api/v1/health</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;success&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1.0.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;containers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;uptime_seconds&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3600</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;config_version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;dry_run&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;detectors&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;miner&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;portscan&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ddos&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;error&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;meta&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1.0.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>/api/v1/health</code> is public and cheap — safe for load-balancer or monitoring probes.</p><h3 id="_2-registration-on-the-node" tabindex="-1">2. Registration (on the node) <a class="header-anchor" href="#_2-registration-on-the-node" aria-label="Permalink to &quot;2. Registration (on the node)&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">journalctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sentinel-node.service</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --since</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> today</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;registered as node&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sentinel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span></span></code></pre></div><p><code>sentinel status</code> prints the config file, state dir, panel URL, API listen address, current config version, dry-run state and docker connectivity.</p><h3 id="_3-panel-side" tabindex="-1">3. Panel side <a class="header-anchor" href="#_3-panel-side" aria-label="Permalink to &quot;3. Panel side&quot;">​</a></h3><p>Open <strong>Sentinel → Nodes</strong>: the node card should show <strong>online</strong>, the agent version, a recent last-seen time, and the current config version with push status. <strong>Sentinel → Dashboard</strong> should count the node under online nodes.</p><h3 id="_4-test-event" tabindex="-1">4. Test event <a class="header-anchor" href="#_4-test-event" aria-label="Permalink to &quot;4. Test event&quot;">​</a></h3><p>Run a one-off local scan (no enforcement, nothing shipped):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sentinel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scan</span></span></code></pre></div><p>Then confirm the live pipeline works end-to-end by watching the log while a detection fires, and check <strong>Sentinel → Detections</strong> for the event.</p><hr><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h2><h3 id="admin-pages-return-404" tabindex="-1">Admin pages return 404 <a class="header-anchor" href="#admin-pages-return-404" aria-label="Permalink to &quot;Admin pages return 404&quot;">​</a></h3><p>The addon&#39;s routes were not registered — usually stale Laravel caches after install. From the panel directory:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route:clear</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config:clear</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cache:clear</span></span></code></pre></div><p>If the sidebar link is missing but routes exist, the sidebar patcher did not apply; re-run the installer (it is idempotent).</p><h3 id="node-shows-offline" tabindex="-1">Node shows offline <a class="header-anchor" href="#node-shows-offline" aria-label="Permalink to &quot;Node shows offline&quot;">​</a></h3><p>Check in order:</p><ol><li><code>systemctl status sentinel-node</code> and <code>journalctl -u sentinel-node.service -n 100</code> on the node.</li><li>Panel URL reachable from the node: <code>curl -sS https://panel.example.com/api/sentinel/node/register</code> (expect a 401/405 JSON error — connectivity is what you are testing, not auth).</li><li>Token correctness: re-view it in <strong>Sentinel → Nodes</strong>. If it was reset, re-run <code>install.sh</code> with the new token.</li><li>Time skew: both hosts should run NTP; the panel marks nodes offline after 120 s without a heartbeat (<code>sentinel.offline_after_seconds</code>).</li></ol><p>The agent retries registration with backoff forever — once the panel is reachable and the token is valid, the node comes online by itself.</p><h3 id="migrations-fail-with-errno-150-foreign-key" tabindex="-1">Migrations fail with errno 150 (foreign key) <a class="header-anchor" href="#migrations-fail-with-errno-150-foreign-key" aria-label="Permalink to &quot;Migrations fail with errno 150 (foreign key)&quot;">​</a></h3><p><code>errno: 150 &quot;Foreign key constraint is incorrectly formed&quot;</code> means the parent table column types do not match. Sentinel&#39;s FK columns are <code>unsignedInteger</code> to match Pterodactyl&#39;s <code>nodes.id</code> / <code>servers.id</code>. This error almost always means the migrations ran against a panel whose core tables use a different engine/collation, or an older partial migration exists. Fix:</p><ol><li>Ensure all core Pterodactyl tables are InnoDB with the panel&#39;s default collation (<code>utf8mb4_unicode_ci</code>).</li><li>Drop any half-created <code>sentinel_*</code> tables, then re-run <code>php artisan migrate --force</code>.</li></ol><h3 id="settings-changes-do-not-appear-stale-views-opcache" tabindex="-1">Settings changes do not appear / stale views (opcache) <a class="header-anchor" href="#settings-changes-do-not-appear-stale-views-opcache" aria-label="Permalink to &quot;Settings changes do not appear / stale views (opcache)&quot;">​</a></h3><p>PHP opcache can serve the old compiled views and config after an install or settings change:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> view:clear</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> config:clear</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># then reload PHP-FPM (or apache) to drop opcache:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> systemctl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> reload</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> php8.2-fpm</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # adjust to your PHP version/SAPI</span></span></code></pre></div><h3 id="node-installed-but-no-detections-ever-appear" tabindex="-1">Node installed but no detections ever appear <a class="header-anchor" href="#node-installed-but-no-detections-ever-appear" aria-label="Permalink to &quot;Node installed but no detections ever appear&quot;">​</a></h3><ul><li>Confirm <code>dry_run</code> is expected — in dry-run, events are still reported, so this is not the cause.</li><li>Check the node log for detector errors: <code>journalctl -u sentinel-node.service -f</code>.</li><li>Run <code>sentinel scan</code> manually; it prints every finding as JSON. If that finds nothing on a node you believe is dirty, the detectors are working and the node is simply clean.</li></ul><hr><h2 id="uninstall" tabindex="-1">Uninstall <a class="header-anchor" href="#uninstall" aria-label="Permalink to &quot;Uninstall&quot;">​</a></h2><h3 id="node-agent" tabindex="-1">Node agent <a class="header-anchor" href="#node-agent" aria-label="Permalink to &quot;Node agent&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> node-module/uninstall.sh</span></span></code></pre></div><p>Stops and disables <code>sentinel-node.service</code> and removes the binary and <code>/etc/sentinel</code>. State under <code>/var/lib/sentinel</code> (spool, quarantine, intel, persisted panel config) is kept unless you pass <code>--purge</code>; pass <code>--keep-config</code> to keep <code>/etc/sentinel</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> node-module/uninstall.sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --purge</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # full removal</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> node-module/uninstall.sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --keep-config</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # keep config + token for reinstall</span></span></code></pre></div><p>Then delete the node&#39;s token card in <strong>Sentinel → Nodes</strong> (or leave it — a reset token can never be used again).</p><h3 id="panel-addon" tabindex="-1">Panel addon <a class="header-anchor" href="#panel-addon" aria-label="Permalink to &quot;Panel addon&quot;">​</a></h3><ul><li><strong>Blueprint:</strong> <code>blueprint -r pterodactylsentinel</code></li><li><strong>Standalone:</strong> <code>sudo PTERODACTYL_DIRECTORY=/var/www/pterodactyl bash standalone/remove.sh</code></li></ul><p>Both remove the addon&#39;s files, routes and sidebar entry. The <code>sentinel_*</code> tables are left in place by default so your event history survives a reinstall; drop them manually if you want a clean slate.</p>`,79)])])}const g=s(n,[["render",l]]);export{k as __pageData,g as default};
