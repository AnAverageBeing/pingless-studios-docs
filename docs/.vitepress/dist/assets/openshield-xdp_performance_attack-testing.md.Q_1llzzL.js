import{_ as t,o as s,c as a,a2 as o}from"./chunks/framework.CZqXuAiL.js";const g=JSON.parse('{"title":"Attack Testing & Detection Analysis","description":"","frontmatter":{},"headers":[],"relativePath":"openshield-xdp/performance/attack-testing.md","filePath":"openshield-xdp/performance/attack-testing.md","lastUpdated":null}'),i={name:"openshield-xdp/performance/attack-testing.md"};function r(d,e,n,l,c,h){return s(),a("div",null,[...e[0]||(e[0]=[o(`<h1 id="attack-testing-detection-analysis" tabindex="-1">Attack Testing &amp; Detection Analysis <a class="header-anchor" href="#attack-testing-detection-analysis" aria-label="Permalink to &quot;Attack Testing &amp; Detection Analysis&quot;">​</a></h1><p>This page documents the results of live attack testing against OpenShield-XDP, the bugs that testing uncovered, and how the dynamic-detection configuration values change firewall behavior in practice. Use it to tune <code>dynamic:</code> thresholds for your own traffic profile.</p><h2 id="test-methodology-and-its-pitfalls" tabindex="-1">Test Methodology (and Its Pitfalls) <a class="header-anchor" href="#test-methodology-and-its-pitfalls" aria-label="Permalink to &quot;Test Methodology (and Its Pitfalls)&quot;">​</a></h2><p>Live DDoS simulation against your own server is harder than it looks. Three traps we hit — you will hit them too:</p><h3 id="_1-docker-bridge-traffic-never-reaches-xdp" tabindex="-1">1. Docker/bridge traffic never reaches XDP <a class="header-anchor" href="#_1-docker-bridge-traffic-never-reaches-xdp" aria-label="Permalink to &quot;1. Docker/bridge traffic never reaches XDP&quot;">​</a></h3><p>Packets sent from a local Docker container to the host&#39;s <strong>own public IP</strong> are delivered locally through the Docker bridge (<code>pterodactyl0</code>, <code>docker0</code>, ...). The kernel sees the destination is local and hands the packet to the local stack — it <strong>never ingresses the physical interface</strong>, so an XDP program attached to <code>eth0</code>/<code>enp3s0</code> cannot see it, let alone count or drop it.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>container → veth → pterodactyl0 → (local delivery) → host stack</span></span>
<span class="line"><span>                                  ✗ enp3s0 XDP sees nothing</span></span></code></pre></div><p>This is expected XDP behavior (XDP hooks are per-interface, at driver ingress), not a firewall bug. Consequence: <strong>container-based attack tests against your own IP are invalid</strong> — you are testing nothing. The same applies to any locally-routed traffic.</p><h3 id="_2-spoofed-source-floods-are-filtered-by-the-sender-s-provider" tabindex="-1">2. Spoofed-source floods are filtered by the <em>sender&#39;s</em> provider <a class="header-anchor" href="#_2-spoofed-source-floods-are-filtered-by-the-sender-s-provider" aria-label="Permalink to &quot;2. Spoofed-source floods are filtered by the *sender&#39;s* provider&quot;">​</a></h3><p><code>hping3 --rand-source</code> from an external VPS produces packets with spoofed sources. Most hosting providers apply egress anti-spoofing (uRPF/ACL), so those packets are dropped before they leave the sender&#39;s network. Your firewall logs show nothing and it looks like &quot;detection is broken&quot; — the traffic simply never arrived.</p><h3 id="_3-vps-to-vps-traffic-may-be-blocked-entirely" tabindex="-1">3. VPS-to-VPS traffic may be blocked entirely <a class="header-anchor" href="#_3-vps-to-vps-traffic-may-be-blocked-entirely" aria-label="Permalink to &quot;3. VPS-to-VPS traffic may be blocked entirely&quot;">​</a></h3><p>Some provider pairs block or heavily filter inter-network traffic (ICMP, UDP, or whole prefixes). Verify plain TCP connectivity between attacker and target <strong>before</strong> drawing conclusions from a missing flood.</p><h3 id="_4-external-floods-can-get-you-banned" tabindex="-1">4. External floods can get YOU banned <a class="header-anchor" href="#_4-external-floods-can-get-you-banned" aria-label="Permalink to &quot;4. External floods can get YOU banned&quot;">​</a></h3><p>Flooding your own server from another host looks like an attack to <strong>both</strong> providers (outbound from the attacker, inbound to you). Don&#39;t do it. Use the internal rig below.</p><h3 id="the-safe-way-an-internal-veth-rig-recommended" tabindex="-1">The safe way: an internal veth rig (recommended) <a class="header-anchor" href="#the-safe-way-an-internal-veth-rig-recommended" aria-label="Permalink to &quot;The safe way: an internal veth rig (recommended)&quot;">​</a></h3><p>All test traffic stays on the host — nothing ever reaches the provider network:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># veth pair + namespace; public-range IPs so the bogon filter doesn&#39;t</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># drop the test traffic (10.x sources are dropped by design).</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> link</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> veth-os0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> type</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> veth</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> peer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> veth-os1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> atk</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> link</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> veth-os0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> atk</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> atk</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> addr</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 11.0.0.1/24</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> veth-os0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> addr</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 11.0.0.2/24</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> veth-os1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> atk</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> link</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> veth-os0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> atk</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> link</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> link</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> veth-os1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Attach the SAME pinned XDP program to the veth end (generic mode).</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># NOTE: loader restarts unload the program — re-attach after each restart.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PID</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bpftool</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> prog</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> show</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;name openshield_xdp&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> head</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> cut</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bpftool</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> net</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> attach</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> xdpgeneric</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $PID </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> veth-os1</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Flood — never leaves the machine:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> atk</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> hping3</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --udp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 53</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> u500</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 11.0.0.2</span></span></code></pre></div><p>Two gotchas we hit: private <code>10.x</code> sources are dropped by the bogon filter (use <code>11.0.0.x</code>), and a loader restart silently invalidates the veth attach (the attach references the old program, which keeps dropping packets into dead maps — detach first, then attach the new program id).</p><p>Also verify arrival in the firewall&#39;s own counters (<code>total_*_packets</code> in <code>global_stats_map</code>, or the telemetry socket) — &quot;packets sent&quot; on the attacker proves nothing.</p><h2 id="bugs-found-by-testing" tabindex="-1">Bugs Found by Testing <a class="header-anchor" href="#bugs-found-by-testing" aria-label="Permalink to &quot;Bugs Found by Testing&quot;">​</a></h2><p>All of these were found by the test campaign and are fixed in the current build.</p><h3 id="f1-—-attack-state-never-cleared-under-attack-for-3-hours" tabindex="-1">F1 — Attack state never cleared (&quot;under attack&quot; for 3+ hours) <a class="header-anchor" href="#f1-—-attack-state-never-cleared-under-attack-for-3-hours" aria-label="Permalink to &quot;F1 — Attack state never cleared (&quot;under attack&quot; for 3+ hours)&quot;">​</a></h3><p><strong>Symptom:</strong> TUI/status showed <code>UNDER ATTACK</code> for hours after traffic returned to normal; Discord &quot;attack ended&quot; webhooks never fired.</p><p><strong>Root cause:</strong> attack recovery required traffic to fall below <code>baseline_pps × spike_recovery_factor</code> (≈ 96 pps on the test server) for <code>spike_recovery_time</code> (60) consecutive seconds. On any server with real background traffic (game servers, panels, API chatter), noise alone exceeds <code>baseline × 1.5</code> regularly, so the recovery counter kept resetting. Attacks only ended via the 300 s absolute timeout, then instantly re-triggered on the next noise burst — a permanent attack state.</p><p><strong>Fix:</strong> recovery is now <strong>spike-relative</strong>: the attack ends when traffic stays below <code>spike_threshold × spike_recovery_factor</code> for <code>spike_recovery_time</code> seconds. The factor must be <code>&lt; 1.0</code> (default <code>0.7</code>) so the recovery band sits below the trigger band. Mitigation (bans, rate limits) is independent of the attack state, so ending the state promptly never weakens protection. New defaults: <code>spike_recovery_factor: 0.7</code>, <code>spike_recovery_time: 10</code>.</p><h3 id="f2-—-end-→-re-detect-loop" tabindex="-1">F2 — End → re-detect loop <a class="header-anchor" href="#f2-—-end-→-re-detect-loop" aria-label="Permalink to &quot;F2 — End → re-detect loop&quot;">​</a></h3><p><strong>Symptom:</strong> after an attack ended (timeout or recovery), the next small burst immediately opened a new attack, so tracker records accumulated multi-hour durations.</p><p><strong>Fix:</strong> 10-second post-attack cooldown — re-triggering within 10 s of an attack end requires a clearly renewed spike (≥ 2× threshold), not hover traffic.</p><h3 id="f3-—-udp-icmp-floods-mis-classified-as-tcp-flood" tabindex="-1">F3 — UDP/ICMP floods mis-classified as TCP_FLOOD <a class="header-anchor" href="#f3-—-udp-icmp-floods-mis-classified-as-tcp-flood" aria-label="Permalink to &quot;F3 — UDP/ICMP floods mis-classified as TCP_FLOOD&quot;">​</a></h3><p><strong>Root cause:</strong> <code>classifyType</code> computed per-protocol deviation from baseline as <code>current / baseline</code>. When the baseline had <strong>zero</strong> of a protocol (a server that never sees UDP/ICMP), deviation stayed 0 and the UDP/ICMP branch silently failed, falling through to the generic TCP/MIXED fallback.</p><p><strong>Fix:</strong> a protocol that is absent from the baseline but present at ≥ 100 pps now counts as effectively infinite deviation, so UDP floods classify as <code>UDP_FLOOD</code>, ICMP floods as <code>ICMP_FLOOD</code> even on servers with zero baseline for that protocol.</p><h3 id="f4-—-one-shot-classification-at-trigger-time" tabindex="-1">F4 — One-shot classification at trigger time <a class="header-anchor" href="#f4-—-one-shot-classification-at-trigger-time" aria-label="Permalink to &quot;F4 — One-shot classification at trigger time&quot;">​</a></h3><p><strong>Root cause:</strong> the attack type was computed once, at trigger time — often mid-ramp when protocol shares are not yet representative.</p><p><strong>Fix:</strong> the classifier re-classifies every 5 seconds during an attack. A type switch requires the same new type 3 consecutive times <strong>and</strong> ≥ 60 s since the last accepted switch (anti-flap). Re-classifications propagate to the tracker, the forensics report, and Discord alerts.</p><h3 id="f5-—-syn-ack-floods-invisible-to-the-classifier" tabindex="-1">F5 — SYN-ACK floods invisible to the classifier <a class="header-anchor" href="#f5-—-syn-ack-floods-invisible-to-the-classifier" aria-label="Permalink to &quot;F5 — SYN-ACK floods invisible to the classifier&quot;">​</a></h3><p><strong>Fix:</strong> added a global <code>total_synack_packets</code> counter to the BPF stats struct; the collector now computes real <code>synack_pps</code>. SYN-ACK reflector floods are now distinguishable from pure SYN floods.</p><h3 id="f6-—-systemd-service-could-not-restart-after-unload-reboot" tabindex="-1">F6 — systemd service could not restart after unload/reboot <a class="header-anchor" href="#f6-—-systemd-service-could-not-restart-after-unload-reboot" aria-label="Permalink to &quot;F6 — systemd service could not restart after unload/reboot&quot;">​</a></h3><p><strong>Symptom:</strong> <code>systemctl start openshield-loader</code> failed with <code>Failed to set up mount namespacing: /sys/fs/bpf/openshield: No such file or directory</code> after <code>openshield unload</code> or a reboot.</p><p><strong>Root cause:</strong> <code>ReadWritePaths=/sys/fs/bpf/openshield /var/run/openshield ...</code> fails the whole mount namespace when a listed path is missing — and BPF pins are removed on unload, <code>/run</code> is tmpfs after reboot.</p><p><strong>Fix:</strong> the unit now uses <code>RuntimeDirectory</code>/<code>StateDirectory</code>/<code>LogsDirectory</code> (auto-created by systemd), <code>ExecStartPre=mkdir -p /sys/fs/bpf/openshield</code>, and <code>ReadWritePaths=/sys/fs/bpf</code>. <code>ProtectKernelTunables</code> had to be dropped — it remounts <code>/sys</code> read-only inside the namespace, breaking both the mkdir and map pinning.</p><h3 id="f7-—-one-stalled-tui-client-could-freeze-telemetry-for-everyone" tabindex="-1">F7 — One stalled TUI client could freeze telemetry for everyone <a class="header-anchor" href="#f7-—-one-stalled-tui-client-could-freeze-telemetry-for-everyone" aria-label="Permalink to &quot;F7 — One stalled TUI client could freeze telemetry for everyone&quot;">​</a></h3><p><strong>Symptom:</strong> a TUI tab stuck on <code>Connected to loader — waiting for telemetry...</code> while another tab worked fine.</p><p><strong>Root cause:</strong> <code>Broadcast</code> wrote snapshots to every subscribed client <strong>synchronously while holding the server mutex</strong>, with a 5 s write deadline per client. One suspended or slow client (Ctrl-Z, laggy SSH session) stalled the whole fan-out for seconds per interval; it could also delay <code>Stop()</code>. There was also a latent data race: request responses were written from the handler goroutine while <code>Broadcast</code> wrote to the same <code>bufio.Writer</code>.</p><p><strong>Fix:</strong> each client now has its own buffered outbound queue (depth 16) drained by a dedicated writer goroutine. <code>Broadcast</code> does a non-blocking send per client — a client that falls behind simply misses intermediate snapshots and can never stall others. All writes are serialized through a per-client mutex, eliminating the race.</p><h3 id="f8-—-mitigation-bypass-against-services-on-ports-32768" tabindex="-1">F8 — Mitigation bypass against services on ports &gt; 32768 <a class="header-anchor" href="#f8-—-mitigation-bypass-against-services-on-ports-32768" aria-label="Permalink to &quot;F8 — Mitigation bypass against services on ports &gt; 32768&quot;">​</a></h3><p><strong>Symptom:</strong> floods aimed at services listening on high ports (custom apps, some game/panel allocations) were never rate-limited and never gained suspicion score — traffic &quot;climbed slowly and never got mitigated&quot;.</p><p><strong>Root cause:</strong> to avoid throttling legitimate outbound traffic (apt, curl, downloads), both <code>stage_rate_limit</code> and the suspicion scorer skipped <strong>every</strong> packet with <code>dport &gt; 32768</code>. That blanket rule also exempted inbound floods targeting high-port services.</p><p><strong>Fix:</strong> a proper <code>is_outbound_response()</code> discriminator now decides what skips scoring/rate-limiting:</p><ul><li><code>dport &lt;= 32768</code> — service port, always inspected.</li><li>TCP <strong>bare SYN</strong> to any port — inbound connection attempt, always inspected (responses never carry SYN without ACK).</li><li>TCP non-SYN to an ephemeral port — treated as outbound response (accepted gap: ACK floods to high-port services are indistinguishable from downloads without connection state).</li><li>UDP to an ephemeral port — treated as a response only when the <strong>source port</strong> is well-known (<code>&lt;= 1024</code>: DNS, NTP). Random-sport UDP floods to high ports are inspected again.</li></ul><p>Legitimate responses (apt from <code>mirror:80 → you:45123</code>, DNS from <code>resolver:53 → you:5xxxx</code>) still bypass mitigation exactly as before.</p><h3 id="f9-—-userspace-map-keys-were-byte-swapped-whitelist-and-manual-bans-never-matched" tabindex="-1">F9 — Userspace map keys were byte-swapped: whitelist and manual bans never matched <a class="header-anchor" href="#f9-—-userspace-map-keys-were-byte-swapped-whitelist-and-manual-bans-never-matched" aria-label="Permalink to &quot;F9 — Userspace map keys were byte-swapped: whitelist and manual bans never matched&quot;">​</a></h3><p><strong>Symptom:</strong> <code>openshield whitelist add &lt;ip&gt;</code> printed success but the IP was <strong>not</strong> actually protected — it could still be scored and banned. Manual <code>blacklist add</code>, forensics-loaded ban lists, GeoIP-triggered bans, and subnet bans had the same problem: inserted, but never matched a single packet.</p><p><strong>Root cause:</strong> the XDP program keys its maps with the on-wire address verbatim (<code>ip-&gt;saddr</code>, network byte order). Every userspace writer (CLI, TUI access handlers, startup whitelist populate, ban manager) instead composed the key as a big-endian integer — which on a little-endian host stores the bytes reversed, so BPF lookups never hit. Auto-bans inserted by the BPF program itself used the correct order — which is also why ban IPs displayed in the TUI/Discord were byte-reversed (the userspace <em>read</em> path made the same mistake in reverse).</p><p><strong>Fix:</strong> canonical <code>bpf.IPToU32</code> / <code>bpf.U32ToIP</code> helpers (network byte order) now used by every writer and reader — whitelist add/remove, blacklist add/remove, CIDR expansion, subnet bans, GeoIP bans, ban events, top-offender and access-list display. The CLI Bloom-filter writer was additionally overwriting whole 64-bit words (erasing other entries&#39; bits) and hashing with a different algorithm than the BPF checker; it now mirrors <code>bloom_hash()</code> exactly and ORs bits in.</p><p><strong>Impact if you used the whitelist before this fix:</strong> your whitelist never worked. Re-add your trusted IPs after upgrading (startup repopulation from config applies the fix automatically). Related gap fixed at the same time: the startup loader silently <strong>skipped CIDR entries</strong> in <code>whitelist.ips</code> (only plain IPs loaded) — CIDRs are now expanded per-IP (bounded at 10,000), matching the CLI behavior.</p><h3 id="f10-—-kernel-and-userspace-fought-over-attack-state-alert-storms-stuck-state" tabindex="-1">F10 — Kernel and userspace fought over <code>attack_state</code> (alert storms, stuck state) <a class="header-anchor" href="#f10-—-kernel-and-userspace-fought-over-attack-state-alert-storms-stuck-state" aria-label="Permalink to &quot;F10 — Kernel and userspace fought over \`attack_state\` (alert storms, stuck state)&quot;">​</a></h3><p><strong>Symptom:</strong> firewall showed <code>UNDER ATTACK</code> (type MIXED) at ~100 pps of pure admin traffic; Discord fired repeated attack alerts; after a loader restart the state could persist indefinitely.</p><p><strong>Root cause:</strong> two kernel-side detectors (entropy spoofing, SYN/FIN ratio) wrote <code>attack_state = 1</code> directly into the baseline map — the SYN/FIN one using <strong>cumulative</strong> counters, so a handful of SSH sessions (SYNs that end in RST, not FIN) tripped it permanently. Meanwhile the userspace baseline writer rebuilt the whole baseline struct every interval, zeroing the state back. The two writers fought: the collector saw repeated 0→1→0 transitions and started/ended attack tracking (and webhooks) over and over. Separately, a loader killed mid-attack left <code>attack_state=1</code> in the pinned map with no one to clear it, since the classifier only writes on transitions.</p><p><strong>Fix:</strong> the userspace attack classifier is now the <strong>single owner</strong> of <code>attack_state</code>:</p><ul><li>Kernel detectors keep their <code>prof</code> counters but no longer write attack state.</li><li>The baseline writer preserves the classifier-owned fields (read-modify-write) instead of rebuilding from scratch.</li><li>Loader startup explicitly resets any stale <code>attack_state</code> left by a previous run.</li></ul><h3 id="f11-—-attack-pps-threshold-could-never-raise-the-trigger" tabindex="-1">F11 — <code>attack_pps_threshold</code> could never <em>raise</em> the trigger <a class="header-anchor" href="#f11-—-attack-pps-threshold-could-never-raise-the-trigger" aria-label="Permalink to &quot;F11 — \`attack_pps_threshold\` could never *raise* the trigger&quot;">​</a></h3><p><strong>Symptom:</strong> setting <code>attack_pps_threshold: 5000</code> to stop false positives had no effect — a 2,000 pps flood still triggered an attack.</p><p><strong>Root cause:</strong> the override only applied when current traffic <strong>already exceeded</strong> the explicit threshold (<code>if currentPPS &gt; attackPPSThreshold { spikePPS = attackPPSThreshold }</code>), so the learned/floored threshold (~1,000) kept triggering first. Verified live: flood at 1,800 pps triggered with threshold=5000 before the fix, and stayed quiet after it.</p><p><strong>Fix:</strong> an explicit threshold now overrides the learned one outright (<code>if attackPPSThreshold &gt; 0 { spikePPS = attackPPSThreshold }</code>).</p><h3 id="f12-—-baseline-inflation-undetected-floods-trained-the-baseline-upward-slow-climb-bypass" tabindex="-1">F12 — Baseline inflation: undetected floods trained the baseline upward (&quot;slow climb&quot; bypass) <a class="header-anchor" href="#f12-—-baseline-inflation-undetected-floods-trained-the-baseline-upward-slow-climb-bypass" aria-label="Permalink to &quot;F12 — Baseline inflation: undetected floods trained the baseline upward (&quot;slow climb&quot; bypass)&quot;">​</a></h3><p><strong>Symptom:</strong> back-to-back or slow-ramping floods progressively stopped being detected — traffic &quot;climbed slowly and shut the attack off&quot;, exactly as reported in production.</p><p><strong>Root cause:</strong> the baseline learner folded <em>all</em> traffic into the EMA whenever <code>attack_state</code> was 0. A flood below the (inflated) trigger never set attack state, so learning continued <strong>through the flood</strong>, inflating the baseline in real time and chasing the spike threshold above the flood rate — the flood made itself permanently invisible. Measured live: baseline 621 → 1,695 (spike threshold 2,483 → 6,778) in 14 seconds of flooding; a 2,836 pps flood then failed to trigger because the spike threshold had reached 2,907.</p><p><strong>Fix:</strong> the learner now never folds in traffic above the <strong>effective trigger ceiling</strong> (<code>max(spike_threshold, attack_min_pps)</code>), regardless of attack state. Organic traffic growth below the trigger is still learned normally. Verified live: with the clamp, the baseline stayed frozen for the whole flood and detection fired in ~2 s.</p><h3 id="f13-—-baseline-anchor-scoring-false-banned-busy-legit-workloads-in-seconds" tabindex="-1">F13 — Baseline-anchor scoring false-banned busy legit workloads in seconds <a class="header-anchor" href="#f13-—-baseline-anchor-scoring-false-banned-busy-legit-workloads-in-seconds" aria-label="Permalink to &quot;F13 — Baseline-anchor scoring false-banned busy legit workloads in seconds&quot;">​</a></h3><p><strong>Symptom:</strong> legitimate game-server traffic (~2,000 pps UDP from one IP — 40 players behind NAT) was banned in ~2 seconds on the Gaming preset, even with correct thresholds and no attack active.</p><p><strong>Root cause:</strong> the baseline-anchored detector awarded partial suspicion (40% of metric score, up to 7 points) to any source exceeding <code>baseline × anchor_mult</code> (3–6×), and it ran at the 256-packet cadence (~8 evaluations/s at 2k pps). On a quiet server (baseline ~75 pps), any busy service exceeds the anchor — accruing ~56+ points/s and banning in ~2 s. Additionally, the attack-state threshold tightening (50%) halved thresholds globally during any attack, so legit sources above the halved threshold were swept up; and <code>attack_min_pps</code> (default 1,000) let a 2k pps game server enter attack state in the first place.</p><p><strong>Fix (validated live — score stays 0 for 2k pps game traffic, floods still ban in ~2 s):</strong></p><ul><li>Anchor partials now fire <strong>only during a declared attack</strong> and <strong>only once per window</strong>, at 10% weight with higher multipliers (5/8/10/12 by tier).</li><li>Profiles now set per-category <code>attack_min_pps</code>/<code>attack_min_bps</code> (Gaming 5,000, Hosting 5,000, CDN 10,000) so normal workload rates never enter attack state.</li><li>New per-category <code>attack_threshold_multiplier</code> (0.7–0.75 for Gaming/Hosting/CDN) so tightening is gentler where legit traffic is heavy.</li><li>Scoring is now <strong>magnitude-scaled</strong> (8× cap): sources far over a threshold ban in 1–2 windows; marginal violators accrue slowly.</li></ul><h3 id="f14-—-global-pps-threshold-below-per-protocol-thresholds-preset-invariant" tabindex="-1">F14 — Global pps threshold below per-protocol thresholds (preset invariant) <a class="header-anchor" href="#f14-—-global-pps-threshold-below-per-protocol-thresholds-preset-invariant" aria-label="Permalink to &quot;F14 — Global pps threshold below per-protocol thresholds (preset invariant)&quot;">​</a></h3><p><strong>Symptom:</strong> a pure-UDP game server exceeded the global <code>pps_threshold</code> (1,200) while staying under <code>udp_pps_threshold</code> (2,000) — the global limit fired first.</p><p><strong>Fix:</strong> preset invariant enforced — global pps ≥ ~1.2× the largest per-protocol threshold for every category (Gaming 3,000, Database 2,000, Hosting 2,000).</p><h3 id="f15-—-xdp-auto-select-preferred-slower-modes" tabindex="-1">F15 — XDP auto-select preferred slower modes <a class="header-anchor" href="#f15-—-xdp-auto-select-preferred-slower-modes" aria-label="Permalink to &quot;F15 — XDP auto-select preferred slower modes&quot;">​</a></h3><p><strong>Fix:</strong> auto attach now tries <strong>offload → native → generic</strong> (earliest drop point first), and the actual attached mode is reported in status/TUI instead of the opaque <code>auto</code>.</p><h2 id="attack-type-matrix-measured-internal-rig" tabindex="-1">Attack Type Matrix (measured, internal rig) <a class="header-anchor" href="#attack-type-matrix-measured-internal-rig" aria-label="Permalink to &quot;Attack Type Matrix (measured, internal rig)&quot;">​</a></h2><p>Each flood ran ~12 s from the veth rig. Detection latency measured from flood start to <code>under_attack</code>; recovery from flood stop to <code>normal</code>.</p><table tabindex="0"><thead><tr><th>Attack</th><th>Rate</th><th>Detected as</th><th>Detection latency</th><th>Recovery</th></tr></thead><tbody><tr><td>SYN flood</td><td>~2,800 pps</td><td><code>SYN_FLOOD</code></td><td>~2–3 s</td><td>~10–12 s</td></tr><tr><td>UDP flood</td><td>~2,800 pps</td><td><code>UDP_FLOOD</code></td><td>~2–3 s</td><td>~10–12 s</td></tr><tr><td>ICMP flood</td><td>~2,700 pps</td><td><code>ICMP_FLOOD</code></td><td>~2–3 s</td><td>~10–12 s</td></tr><tr><td>Mixed SYN+UDP</td><td>~3,200 pps</td><td><code>MIXED</code></td><td>~2–3 s</td><td>~10–12 s</td></tr><tr><td>SYN-ACK reflector</td><td>~2,800 pps</td><td><code>MIXED</code> (via SYN-ACK counter)</td><td>~2–3 s</td><td>~10–12 s</td></tr><tr><td>Fragmented UDP</td><td>~1,800 pps</td><td><code>UDP_FLOOD</code></td><td>~5 s</td><td>~10–12 s</td></tr><tr><td>Heavy UDP</td><td>~7,400 pps</td><td><code>UDP_FLOOD</code></td><td>~2 s</td><td>~10 s</td></tr><tr><td>Below explicit threshold (1,800 pps, threshold=5000)</td><td>1,800 pps</td><td>— (no attack, correct)</td><td>—</td><td>—</td></tr></tbody></table><p>In every case the attacking source was banned within ~1–2 s by the per-source engine, independently of the global attack state.</p><p><strong>CPU cost during a 7,200 pps flood (single vCPU, shared VPS):</strong> loader process 0.3% CPU, softirq time 0.0% — the XDP path cost is negligible at these rates; detection and banning added no measurable load.</p><h2 id="configuration-parameter-impact-analysis-measured" tabindex="-1">Configuration Parameter Impact Analysis (measured) <a class="header-anchor" href="#configuration-parameter-impact-analysis-measured" aria-label="Permalink to &quot;Configuration Parameter Impact Analysis (measured)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Parameter</th><th>Value tested</th><th>Observed behavior</th></tr></thead><tbody><tr><td><code>attack_pps_threshold</code></td><td><code>5000</code> with 1,800 pps flood</td><td><strong>Before F11 fix:</strong> triggered anyway (bug). <strong>After:</strong> no attack — threshold respected. Packets still counted, source still banned per-source.</td></tr><tr><td><code>attack_pps_threshold</code></td><td><code>1000</code>–<code>2000</code> with 1,800–7,400 pps floods</td><td>Triggers in 2–3 s as expected.</td></tr><tr><td><code>attack_trigger_time</code></td><td><code>3</code> (default)</td><td>Detection 2–3 s after flood start.</td></tr><tr><td><code>attack_trigger_time</code></td><td><code>6</code> (hot-reload)</td><td>Detection ~5–6 s after flood start; a 4 s flood never triggered. Runtime reload works, no restart needed.</td></tr><tr><td><code>spike_recovery_factor</code></td><td><code>0.7</code> (default)</td><td>Attack state clears ~10–12 s after flood stops.</td></tr><tr><td><code>spike_recovery_factor</code></td><td><code>0.9</code></td><td>State holds slightly longer (recovery band sits closer to the trigger); still clears promptly after the tail drops below <code>0.9 × spike</code>.</td></tr><tr><td><code>spike_recovery_time</code></td><td><code>10</code> (new default)</td><td>State clears ~10 s after traffic falls into the recovery band. Old <code>60</code> + baseline-relative recovery = hours of stuck state (F1).</td></tr><tr><td><code>attack_max_duration</code></td><td><code>300</code></td><td>Hard cap works; post-end cooldown (10 s) stops end→re-detect loops (F2).</td></tr><tr><td>baseline learning</td><td>floods below trigger</td><td><strong>Before F12 fix:</strong> baseline inflated 2–3× in seconds, threshold chased the flood up, later floods invisible. <strong>After:</strong> baseline frozen during floods.</td></tr></tbody></table><h3 id="false-positives-you-should-expect" tabindex="-1">False positives you should expect <a class="header-anchor" href="#false-positives-you-should-expect" aria-label="Permalink to &quot;False positives you should expect&quot;">​</a></h3><p><strong>High-speed downloads look like floods.</strong> A <code>apt</code>/<code>wget</code> at 5–8k pps from 2–3 mirror IPs trips the spike detector (<code>TCP_FLOOD</code>) on a server whose baseline is ~60 pps. This is inherent to any spike detector on a quiet server. Mitigations:</p><ul><li>Raise <code>attack_pps_threshold</code> to a value above your normal burst level (e.g. <code>2000</code>–<code>5000</code>) — now actually works (F11).</li><li>Raise <code>attack_trigger_time</code> to <code>5</code>–<code>8</code> so short downloads never trip it.</li><li>Whitelist your package mirrors/CDN ranges.</li><li>Note that detection does not equal impact: state changes and alerts fire, but legit flows only get dropped if they also trip the per-source suspicion engine.</li></ul><h2 id="recommended-profiles" tabindex="-1">Recommended Profiles <a class="header-anchor" href="#recommended-profiles" aria-label="Permalink to &quot;Recommended Profiles&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Server type</th><th><code>attack_pps_threshold</code></th><th><code>attack_trigger_time</code></th><th><code>spike_recovery_factor</code></th><th><code>spike_recovery_time</code></th></tr></thead><tbody><tr><td>Quiet (APIs, small sites)</td><td><code>0</code> (auto)</td><td><code>3</code></td><td><code>0.7</code></td><td><code>10</code></td></tr><tr><td>Game server (noisy baseline)</td><td><code>2000</code>–<code>3000</code></td><td><code>4</code></td><td><code>0.7</code></td><td><code>10</code></td></tr><tr><td>Heavy download/CDN origin</td><td><code>5000</code>+</td><td><code>6</code></td><td><code>0.6</code></td><td><code>15</code></td></tr></tbody></table><h2 id="preset-values-v1-6-1-validated-by-simulation" tabindex="-1">Preset Values (v1.6.1, validated by simulation) <a class="header-anchor" href="#preset-values-v1-6-1-validated-by-simulation" aria-label="Permalink to &quot;Preset Values (v1.6.1, validated by simulation)&quot;">​</a></h2><p>Per-IP thresholds at <code>Balanced</code> level, from live simulation of each workload at NAT scale (all legit workloads passed with zero scoring; floods banned in ~2 s):</p><table tabindex="0"><thead><tr><th>Category</th><th>pps</th><th>bps</th><th>tcp_pps</th><th>udp_pps</th><th>syn_pps</th><th>susp</th><th>attack_min_pps</th><th>attack tightening</th></tr></thead><tbody><tr><td>Ultra Strict</td><td>250</td><td>2 MB/s</td><td>150</td><td>100</td><td>50</td><td>40</td><td>500</td><td>0.40×</td></tr><tr><td>Strict</td><td>350</td><td>3 MB/s</td><td>225</td><td>175</td><td>75</td><td>50</td><td>800</td><td>0.45×</td></tr><tr><td>Balanced (default)</td><td>500</td><td>6 MB/s</td><td>375</td><td>300</td><td>120</td><td>80</td><td>2,000</td><td>0.50×</td></tr><tr><td>Performance</td><td>850</td><td>12 MB/s</td><td>640</td><td>600</td><td>250</td><td>120</td><td>3,000</td><td>0.60×</td></tr><tr><td>Hosting</td><td>2,000</td><td>24 MB/s</td><td>1,200</td><td>1,200</td><td>500</td><td>200</td><td>5,000</td><td>0.70×</td></tr><tr><td>Gaming</td><td>3,000</td><td>18 MB/s</td><td>1,500</td><td>2,500</td><td>350</td><td>150</td><td>5,000</td><td>0.75×</td></tr><tr><td>Enterprise</td><td>700</td><td>10 MB/s</td><td>525</td><td>450</td><td>170</td><td>100</td><td>2,000</td><td>0.50×</td></tr><tr><td>CDN / Edge</td><td>2,500</td><td>40 MB/s</td><td>2,000</td><td>1,800</td><td>800</td><td>300</td><td>10,000</td><td>0.70×</td></tr><tr><td>Database</td><td>2,000</td><td>24 MB/s</td><td>1,500</td><td>280</td><td>100</td><td>90</td><td>3,000</td><td>0.60×</td></tr></tbody></table><p>Key tuning rules learned from simulation:</p><ul><li><strong>Global pps ≥ 1.2× the largest per-protocol threshold</strong> — or UDP/TCP-heavy services trip the global limit first.</li><li><strong><code>attack_min_pps</code> must exceed the workload&#39;s normal rate</strong> — a 2k pps game server at the old 1k default entered attack state, halved thresholds, and mass-banned its own players.</li><li><strong>NAT aggregation is per-IP reality</strong>: N players behind one IP multiply per-source rates — Gaming/Hosting thresholds assume up to ~50 clients per IP.</li><li>Detection latency is ~2–3 s for clear floods (magnitude-scaled scoring), ~30–60 s for low-and-slow (anchor, attack-state only).</li></ul><h2 id="operational-notes-from-live-testing" tabindex="-1">Operational Notes from Live Testing <a class="header-anchor" href="#operational-notes-from-live-testing" aria-label="Permalink to &quot;Operational Notes from Live Testing&quot;">​</a></h2><ul><li><strong>Whitelist your admin IPs before anything else.</strong> With <code>ban_duration: 3600</code> (1 h default), a burst of SSH connections, an SCP transfer, or a test flood from your own IP earns a one-hour ban — locking you out mid-session. Transfers to the server at line rate can themselves trip per-source scoring; throttle (<code>pv -L 100k</code>) when pushing large files to a protected host.</li><li><strong>Docker/bridge traffic to the host&#39;s own IP bypasses XDP entirely</strong> (see methodology) — XDP only protects traffic that physically ingresses the attached interface. Services reached only via a bridge need the program attached to that bridge instead (separate instance).</li><li>After changing <code>dynamic:</code> detection values, use <code>openshield-loader reload</code> — the detection fields are runtime-safe, no restart needed.</li></ul><h2 id="rotating-spoofed-floods-outlived-the-scoring-window-fixed-in-v1-9-2" tabindex="-1">Rotating spoofed floods outlived the scoring window (fixed in v1.9.2) <a class="header-anchor" href="#rotating-spoofed-floods-outlived-the-scoring-window-fixed-in-v1-9-2" aria-label="Permalink to &quot;Rotating spoofed floods outlived the scoring window (fixed in v1.9.2)&quot;">​</a></h2><p><strong>Observed:</strong> 290K pps UDP flood on Hosting Medium — <code>UNDER ATTACK</code> declared, yet ~98.5% of traffic passed, <code>Suspicious: 0</code>, <code>Bans: 0</code>. Sources rotated every few seconds at ~5K pps each, so per-IP suspicion scoring (which needs multiple windows) never reached the ban threshold for any of them.</p><p><strong>Root cause:</strong> the dataplane had no attack-mode response — peacetime thresholds (tightened by <code>attack_threshold_multiplier</code>) still require several seconds per source, which rotating spoofed sources never provide.</p><p><strong>Fix:</strong> new hard cap <code>dynamic.attack_per_ip_pps</code> (default 1000, per-category in presets: Ultra Strict 400, Gaming 800, Balanced/Enterprise/Custom 1000, Hosting 1500, Performance 2000, CDN/Edge 3000, Database 800). While an attack is active, any source above the cap has its excess dropped instantly at XDP — no scoring wait. Verified live: drop rate jumped 0.1% → ~35% the moment the attack was declared, before any bans.</p><p><strong>The rate-limit model, peacetime vs attack:</strong></p><ol><li><strong>Peacetime</strong> — <code>static.*_threshold</code>: sources over these accrue suspicion → ban.</li><li><strong>During attack</strong> — thresholds tightened by <code>attack_threshold_multiplier</code> (scoring starts sooner).</li><li><strong>During attack</strong> — <code>attack_per_ip_pps</code>: the hard cap, immediate XDP drop, no scoring.</li></ol>`,103)])])}const k=t(i,[["render",r]]);export{g as __pageData,k as default};
