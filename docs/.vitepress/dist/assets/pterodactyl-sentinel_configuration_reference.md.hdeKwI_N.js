import{_ as t,o as d,c as o,a2 as s}from"./chunks/framework.CZqXuAiL.js";const u=JSON.parse('{"title":"Configuration Reference","description":"Every Sentinel configuration value — the panel-managed node config tree (general, all 12 detectors, whitelist, rules, intel, limits), alert channels, per-node overrides, and the node-local config.yaml.","frontmatter":{"title":"Configuration Reference","description":"Every Sentinel configuration value — the panel-managed node config tree (general, all 12 detectors, whitelist, rules, intel, limits), alert channels, per-node overrides, and the node-local config.yaml."},"headers":[],"relativePath":"pterodactyl-sentinel/configuration/reference.md","filePath":"pterodactyl-sentinel/configuration/reference.md","lastUpdated":1785525039000}'),a={name:"pterodactyl-sentinel/configuration/reference.md"};function i(c,e,r,n,l,h){return d(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="configuration-reference" tabindex="-1">Configuration Reference <a class="header-anchor" href="#configuration-reference" aria-label="Permalink to &quot;Configuration Reference&quot;">​</a></h1><p>Sentinel has three layers of configuration:</p><ol><li><strong>Panel-managed node config</strong> — the tree documented below. Edited once in <strong>Sentinel → Settings</strong>, versioned, and pushed to every node. This is where all detection behavior lives.</li><li><strong>Panel-side settings</strong> — alert channels (Discord/webhook/SMTP) and per-node overrides. These live only in the panel database and are <strong>never</strong> sent to nodes.</li><li><strong>Node-local <code>/etc/sentinel/config.yaml</code></strong> — written once by <code>install.sh</code>; holds only connectivity settings (panel URL, token file, API listen, paths).</li></ol><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Do not hand-edit detection settings on the node. The agent persists the last panel-pushed config under <code>/var/lib/sentinel</code> and re-applies it on boot; local edits to detection knobs are overwritten on the next config sync. Make changes in the panel.</p></div><p>How values reach a node: on every save the panel increments <code>config_version</code> and pushes the full tree (<code>POST /api/v1/config</code>); a node whose heartbeat reports a stale version pulls <code>GET /api/sentinel/node/config</code>. Apply is atomic — the node validates the whole tree, writes a temp file, renames it into place, and reloads. A config that fails validation is rejected entirely; the last-known-good config keeps running.</p><hr><h2 id="general" tabindex="-1">general <a class="header-anchor" href="#general" aria-label="Permalink to &quot;general&quot;">​</a></h2><p>Top-level engine behavior.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>general.scan_interval_seconds</code></td><td>int</td><td><code>5</code></td><td>Seconds between detection ticks. Each tick takes one shared system snapshot (processes, per-netns connections) that all fast detectors consume. Range 1–3600.</td></tr><tr><td><code>general.cooldown_seconds</code></td><td>int</td><td><code>300</code></td><td>Per-event dedup window. An identical detection (same detector + target) is suppressed for this long so one miner does not produce an event every tick. <code>0</code> disables dedup.</td></tr><tr><td><code>general.dry_run</code></td><td>bool</td><td><code>true</code></td><td>Global safe mode. Detections are logged and reported to the panel, but no local action runs; events carry <code>dry_run: true</code> and empty <code>actions_taken</code>. Panel-side suspension is also suppressed.</td></tr><tr><td><code>general.log_level</code></td><td>string</td><td><code>info</code></td><td>Agent log verbosity: <code>debug</code>, <code>info</code>, <code>warn</code>, <code>error</code>.</td></tr></tbody></table><p><strong>When to change.</strong> Lower <code>scan_interval_seconds</code> only if you need faster reaction — 5 s is already aggressive and each tick is cheap. Raise <code>cooldown_seconds</code> on noisy nodes; lower it while testing so repeat detections re-fire quickly.</p><p><strong>Common mistakes.</strong> Disabling dry-run before reviewing what the rules would do; setting <code>log_level: debug</code> in production and filling the journal with per-tick detail; setting <code>cooldown_seconds: 0</code> and drowning the panel in duplicate events (the <code>limits.max_events_per_minute</code> cap will then drop events).</p><hr><h2 id="detectors" tabindex="-1">detectors <a class="header-anchor" href="#detectors" aria-label="Permalink to &quot;detectors&quot;">​</a></h2><p>Each detector has an <code>enabled</code> switch plus its own knobs. Defaults below come from the agent&#39;s built-in defaults (<code>internal/config/defaults.go</code>); the panel settings form exposes the same tree, and any knob the panel does not render keeps its built-in value via deep-merge.</p><h3 id="detectors-miner" tabindex="-1">detectors.miner <a class="header-anchor" href="#detectors-miner" aria-label="Permalink to &quot;detectors.miner&quot;">​</a></h3><p>Cryptominer detection via process signatures, CPU heuristics and pool connections. Category: <code>miner</code>.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>detectors.miner.enabled</code></td><td>bool</td><td><code>true</code></td><td>Master switch.</td></tr><tr><td><code>detectors.miner.cpu_threshold</code></td><td>int</td><td><code>85</code></td><td>CPU percent (single-core scale; up to <code>100 * 64</code>) a process must sustain to be flagged by the heuristic.</td></tr><tr><td><code>detectors.miner.sustained_seconds</code></td><td>int</td><td><code>45</code></td><td>How long CPU must stay above the threshold before the heuristic fires.</td></tr><tr><td><code>detectors.miner.known_processes</code></td><td>list</td><td>23 names</td><td>Known mining binaries: <code>xmrig</code>, <code>minerd</code>, <code>cpuminer</code>, <code>cpuminer-avx2</code>, <code>ccminer</code>, <code>ethminer</code>, <code>cgminer</code>, <code>bfgminer</code>, <code>nbminer</code>, <code>t-rex</code>, <code>trex</code>, <code>phoenixminer</code>, <code>lolminer</code>, <code>gminer</code>, <code>nanominer</code>, <code>xmr-stak</code>, <code>teamredminer</code>, <code>srbminer</code>, <code>wildrig</code>, <code>verusminer</code>, <code>kawpowminer</code>, <code>miniz</code>, <code>rigel</code>. Match → high severity.</td></tr><tr><td><code>detectors.miner.pool_ports</code></td><td>int list</td><td><code>3333 4444 5555 7777 8888 9999 14433 14444 20580 45560 45700</code></td><td>TCP ports commonly used by mining pools. An outbound connection to one → high-severity &quot;connection to mining pool port&quot;.</td></tr><tr><td><code>detectors.miner.whitelist_processes</code></td><td>list</td><td>game-server binaries</td><td>Processes exempt from CPU/connection heuristics (signatures still apply): <code>java</code>, <code>bedrock_server</code>, <code>srcds_linux</code>, <code>srcds_run</code>, <code>hlds_linux</code>, <code>hlds_run</code>, <code>arma3server</code>, <code>arma3server_x64</code>, <code>rustdedicated</code>, <code>factorio</code>, <code>terraria-server</code>, <code>tshock</code>, <code>fivem</code>, <code>altv-server</code>, <code>samp03svr</code>, <code>mta-server</code>, <code>ragemp</code>, <code>ragemp-server</code>, <code>unturned</code>, <code>unturned_headless</code>, <code>vrising-server</code>, <code>valheim_server</code>, <code>valheim_server.x86_64</code>, <code>cs2</code>, <code>csgo</code>, <code>dota2</code>, <code>tf2</code>, <code>gmod</code>.</td></tr></tbody></table><p>Severity escalation: sustained high CPU → medium; known miner binary or deleted-binary masked process → high; mining argument flags in the cmdline (stratum URLs, <code>--donate-level</code>, <code>--coin</code>, <code>--randomx</code>, <code>--algo</code>, <code>--nicehash</code>) → critical.</p><p><strong>When to change.</strong> Add new miner names or pool ports as you encounter them. Do not lower <code>cpu_threshold</code> below ~70 — busy game servers will false-positive (they are whitelisted, but custom eggs may not be).</p><h3 id="detectors-portscan" tabindex="-1">detectors.portscan <a class="header-anchor" href="#detectors-portscan" aria-label="Permalink to &quot;detectors.portscan&quot;">​</a></h3><p>Outbound network scanning from host or containers. Category: <code>portscan</code>.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>detectors.portscan.enabled</code></td><td>bool</td><td><code>true</code></td><td>Master switch.</td></tr><tr><td><code>detectors.portscan.distinct_ports</code></td><td>int</td><td><code>100</code></td><td>Distinct destination ports within the window that indicate a scan.</td></tr><tr><td><code>detectors.portscan.distinct_hosts</code></td><td>int</td><td><code>50</code></td><td>Distinct destination hosts within the window that indicate a scan.</td></tr><tr><td><code>detectors.portscan.window_seconds</code></td><td>int</td><td><code>15</code></td><td>Sliding window for the distinct-port/host counts.</td></tr><tr><td><code>detectors.portscan.known_scanners</code></td><td>list</td><td><code>nmap masscan zmap unicornscan hping3 naabu rustscan</code></td><td>Process names that are flagged immediately regardless of counts.</td></tr></tbody></table><p>Severity: medium at threshold, high when well past it. The default rule is alert-only — port scanning is reconnaissance, not damage.</p><h3 id="detectors-ddos" tabindex="-1">detectors.ddos <a class="header-anchor" href="#detectors-ddos" aria-label="Permalink to &quot;detectors.ddos&quot;">​</a></h3><p>Outbound flood/stress activity. Category: <code>ddos</code>.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>detectors.ddos.enabled</code></td><td>bool</td><td><code>true</code></td><td>Master switch.</td></tr><tr><td><code>detectors.ddos.pps_threshold</code></td><td>int</td><td><code>60000</code></td><td>Packets-per-second from one container that indicate a flood.</td></tr><tr><td><code>detectors.ddos.bps_threshold</code></td><td>int</td><td><code>125000000</code></td><td>Bits-per-second (~1 Gbit/s) from one container that indicate a flood.</td></tr><tr><td><code>detectors.ddos.conn_threshold</code></td><td>int</td><td><code>1500</code></td><td>Concurrent outbound connections from one container that indicate a connection flood.</td></tr><tr><td><code>detectors.ddos.known_tools</code></td><td>list</td><td>15 names</td><td>Stress-tool binaries, matched on word boundaries: <code>hping3</code>, <code>t50</code>, <code>mhddos</code>, <code>ufonet</code>, <code>slowloris</code>, <code>goldeneye</code>, <code>torshammer</code>, <code>xerxes</code>, <code>ipstresser</code>, <code>raven-storm</code>, <code>pyflood</code>, <code>hoic</code>, <code>loic</code>, <code>xoic</code>, <code>hulkattack</code>.</td></tr><tr><td><code>detectors.ddos.whitelist_processes</code></td><td>list</td><td>game-server binaries</td><td>Same game-server list as the miner detector — legit game traffic is exempt from rate heuristics.</td></tr></tbody></table><p>All flood detections are high severity. The default rule pauses the container and suspends the server.</p><p><strong>Common mistakes.</strong> Dropping <code>pps_threshold</code> to catch small floods — game servers (especially Rust, FiveM, Minecraft proxies) can burst hard; tune down gradually with dry-run on.</p><h3 id="detectors-zipbomb" tabindex="-1">detectors.zipbomb <a class="header-anchor" href="#detectors-zipbomb" aria-label="Permalink to &quot;detectors.zipbomb&quot;">​</a></h3><p>Archive bombs being uploaded or extracted in server volumes. Category: <code>zipbomb</code>.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>detectors.zipbomb.enabled</code></td><td>bool</td><td><code>true</code></td><td>Master switch.</td></tr><tr><td><code>detectors.zipbomb.scan_paths</code></td><td>list</td><td><code>/var/lib/pterodactyl/volumes</code></td><td>Roots swept for suspicious archives.</td></tr><tr><td><code>detectors.zipbomb.ratio_threshold</code></td><td>int</td><td><code>150</code></td><td>Uncompressed:compressed size ratio that flags an archive.</td></tr><tr><td><code>detectors.zipbomb.max_uncompressed_mb</code></td><td>int</td><td><code>51200</code></td><td>Absolute uncompressed size (50 GiB) that flags an archive regardless of ratio.</td></tr><tr><td><code>detectors.zipbomb.full_scan_interval_minutes</code></td><td>int</td><td><code>30</code></td><td>Minutes between full volume sweeps.</td></tr><tr><td><code>detectors.zipbomb.sweep_max_seconds</code></td><td>int</td><td><code>300</code></td><td>Time budget for one sweep.</td></tr><tr><td><code>detectors.zipbomb.inspect_timeout_seconds</code></td><td>int</td><td><code>5</code></td><td>Per-archive inspection timeout.</td></tr><tr><td><code>detectors.zipbomb.workers</code></td><td>int</td><td><code>4</code></td><td>Parallel archive inspectors.</td></tr><tr><td><code>detectors.zipbomb.hot_trigger</code></td><td>bool</td><td><code>true</code></td><td>Enable the reactive trigger: a container writing hot gets its new archives inspected immediately.</td></tr><tr><td><code>detectors.zipbomb.hot_cpu_percent</code></td><td>int</td><td><code>80</code></td><td>CPU percent component of the hot trigger.</td></tr><tr><td><code>detectors.zipbomb.hot_write_mbps</code></td><td>int</td><td><code>25</code></td><td>Disk write MB/s component of the hot trigger.</td></tr></tbody></table><p>Severity: medium at the ratio threshold, high for extreme ratios/sizes. Default rule quarantines the file from medium up.</p><h3 id="detectors-exploit" tabindex="-1">detectors.exploit <a class="header-anchor" href="#detectors-exploit" aria-label="Permalink to &quot;detectors.exploit&quot;">​</a></h3><p>Privilege escalation, container escapes and reverse shells. Category: <code>exploit</code>.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>detectors.exploit.enabled</code></td><td>bool</td><td><code>true</code></td><td>Master switch.</td></tr><tr><td><code>detectors.exploit.watch_paths</code></td><td>list</td><td><code>/tmp /dev/shm /var/tmp</code></td><td>World-writable scratch dirs watched for executions and setuid drops.</td></tr><tr><td><code>detectors.exploit.suspicious_procs</code></td><td>list</td><td><code>dirtycow dirtypipe pwnkit linpeas linenum les.sh unix-privesc-check exploit nsenter deepce</code></td><td>Privesc/escape tools and enumeration scripts. <code>runc</code> is deliberately absent — it spawns every container; escape attempts are caught by the <code>nsenter --target 1</code> cmdline pattern instead.</td></tr><tr><td><code>detectors.exploit.flag_reverse_shell</code></td><td>bool</td><td><code>true</code></td><td>Flag shell processes whose stdio is attached to a socket.</td></tr><tr><td><code>detectors.exploit.flag_privesc</code></td><td>bool</td><td><code>true</code></td><td>Flag privilege-boundary crossings (unexpected uid/capability gains).</td></tr><tr><td><code>detectors.exploit.max_setuid_walk_size</code></td><td>int</td><td><code>200000</code></td><td>Byte budget for the setuid walk in watched paths.</td></tr></tbody></table><p>Severity: privesc tools and reverse shells → high; container-escape patterns (<code>nsenter --target 1</code>) → critical; execution from a scratch dir → medium.</p><h3 id="detectors-abuse" tabindex="-1">detectors.abuse <a class="header-anchor" href="#detectors-abuse" aria-label="Permalink to &quot;detectors.abuse&quot;">​</a></h3><p>Hosting-abuse services: tor, proxies, VPNs, tunnels, mailers, IRC. Category: <code>abuse</code>.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>detectors.abuse.enabled</code></td><td>bool</td><td><code>true</code></td><td>Master switch.</td></tr><tr><td><code>detectors.abuse.known_processes</code></td><td>list</td><td>31 names</td><td><code>tor</code>, <code>torrc</code>, <code>openvpn</code>, <code>wireguard</code>, <code>softether</code>, <code>shadowsocks</code>, <code>ss-server</code>, <code>ss-local</code>, <code>v2ray</code>, <code>xray</code>, <code>trojan</code>, <code>trojan-go</code>, <code>hysteria</code>, <code>hysteria2</code>, <code>sing-box</code>, <code>cloudflared</code>, <code>frpc</code>, <code>frps</code>, <code>ngrok</code>, <code>localtunnel</code>, <code>pagekite</code>, <code>pyload</code>, <code>sendmail</code>, <code>postfix</code>, <code>exim4</code>, <code>exim</code>, <code>dovecot</code>, <code>rspamd</code>, <code>amavis</code>, <code>iredapd</code>, <code>znc</code>, <code>eggdrop</code>.</td></tr><tr><td><code>detectors.abuse.known_cmd_patterns</code></td><td>list</td><td>16 fragments</td><td>Cmdline fragments: <code>--torrc-file</code>, <code>--socksport</code>, <code>--orport</code>, <code>--dirport</code>, <code>--socks-port</code>, <code>--or-port</code>, <code>--dir-port</code>, <code>ss-server</code>, <code>ss-local</code>, <code>-c /etc/tor</code>, <code>-f /etc/tor</code>, <code>/etc/openvpn</code>, <code>/etc/wireguard</code>, <code>--protocol trojan</code>, <code>--protocol vmess</code>, <code>--protocol vless</code>, <code>inbound:</code>, <code>outbound:</code>, <code>listen=0.0.0.0</code>.</td></tr><tr><td><code>detectors.abuse.abusive_ports</code></td><td>int list</td><td>24 ports</td><td>Listening ports associated with tor (<code>9001 9030 9050 9051 9150 9151</code>), socks (<code>1080 1081 1090</code>), HTTP proxies (<code>3128 8118</code>), proxy panels (<code>7890 7891 8000 8001 8008 8082</code>), shadowsocks (<code>8388 8389</code>), trojan/xray fallbacks (<code>4433 8443</code>), hysteria/sing-box defaults (<code>10086 12345 54321</code>), IRC (<code>6667 6697</code>).</td></tr><tr><td><code>detectors.abuse.watch_upload_paths</code></td><td>list</td><td><code>/var/lib/pterodactyl/volumes /var/lib/pterodactyl/mounts /tmp /var/tmp /dev/shm</code></td><td>Customer-writable roots; executing from one is itself a medium-severity signal.</td></tr><tr><td><code>detectors.abuse.whitelist_processes</code></td><td>list</td><td>game-server binaries</td><td>Same game-server exemption list.</td></tr></tbody></table><p>Severity: known abuse process or listener on an abusive port → high; execution from an upload directory → medium.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p><code>cloudflared</code>, <code>frpc</code>/<code>frps</code> and <code>ngrok</code> are flagged by default. If you legitimately sell tunneling or use cloudflared for panel access on the same host, whitelist those paths or mute the affected servers — do not disable the whole detector.</p></div><h3 id="detectors-onaccess" tabindex="-1">detectors.onaccess <a class="header-anchor" href="#detectors-onaccess" aria-label="Permalink to &quot;detectors.onaccess&quot;">​</a></h3><p>Real-time malware scanning of files as they appear in volumes. Category: <code>malware</code>.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>detectors.onaccess.enabled</code></td><td>bool</td><td><code>true</code></td><td>Master switch.</td></tr><tr><td><code>detectors.onaccess.watch_paths</code></td><td>list</td><td><code>/var/lib/pterodactyl/volumes</code></td><td>fsnotify roots — every file create/write/move under these is scanned.</td></tr><tr><td><code>detectors.onaccess.hash_check</code></td><td>bool</td><td><code>true</code></td><td>SHA-256 every new file and check it against the blocklist (local + panel-distributed confirmed hashes).</td></tr><tr><td><code>detectors.onaccess.yara_check</code></td><td>bool</td><td><code>true</code></td><td>Scan new files with the active YARA rule bundle.</td></tr><tr><td><code>detectors.onaccess.settle_ms</code></td><td>int</td><td><code>500</code></td><td>Milliseconds to wait after the last write before scanning, so a file still being uploaded is not scanned half-written.</td></tr></tbody></table><p>Severity: blocklist hash hit → critical; YARA match → high. Default rule quarantines the file. Interesting files are hashed and submitted to the panel intel DB.</p><p><strong>Common mistakes.</strong> Setting <code>settle_ms</code> too low (scans partial uploads, misses the final hash) or disabling <code>hash_check</code> and losing the fleet-wide intel benefit.</p><h3 id="detectors-yara" tabindex="-1">detectors.yara <a class="header-anchor" href="#detectors-yara" aria-label="Permalink to &quot;detectors.yara&quot;">​</a></h3><p>Scheduled YARA sweep across volume trees, complementing the on-access scanner. Category: <code>malware</code>. Disabled by default — the on-access path already covers new files; enable the sweep to catch files written before deployment.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>detectors.yara.enabled</code></td><td>bool</td><td><code>false</code></td><td>Master switch.</td></tr><tr><td><code>detectors.yara.scan_paths</code></td><td>list</td><td><code>/var/lib/pterodactyl/volumes</code></td><td>Roots for the scheduled sweep.</td></tr><tr><td><code>detectors.yara.interval_minutes</code></td><td>int</td><td><code>10</code></td><td>Minutes between sweeps.</td></tr></tbody></table><p>YARA hits are high severity, category <code>malware</code> — consistent with on-access, so one rule covers both. Rules are edited in the panel (<strong>Settings → Detectors</strong> bundle editor) and hot-reloaded on every node.</p><h3 id="detectors-fim" tabindex="-1">detectors.fim <a class="header-anchor" href="#detectors-fim" aria-label="Permalink to &quot;detectors.fim&quot;">​</a></h3><p>File-integrity monitoring. Category: <code>fim</code>. Disabled by default; the shipped default watches the agent&#39;s own config.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>detectors.fim.enabled</code></td><td>bool</td><td><code>false</code></td><td>Master switch.</td></tr><tr><td><code>detectors.fim.paths</code></td><td>list</td><td><code>/etc/sentinel/config.yaml</code></td><td>Files/directories to baseline (SHA-256) and watch.</td></tr><tr><td><code>detectors.fim.interval_minutes</code></td><td>int</td><td><code>5</code></td><td>Minutes between integrity checks.</td></tr></tbody></table><p>Every change to a protected file raises one high-severity event. Useful for watching Wings config, host SSH keys, or critical panel mounts — not for game volumes (they change constantly; that is what on-access is for).</p><h3 id="detectors-trivy" tabindex="-1">detectors.trivy <a class="header-anchor" href="#detectors-trivy" aria-label="Permalink to &quot;detectors.trivy&quot;">​</a></h3><p>Container image CVE scanning via the external <code>trivy</code> binary. Category: <code>vuln</code>. Disabled by default; requires <code>trivy</code> installed on the node.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>detectors.trivy.enabled</code></td><td>bool</td><td><code>false</code></td><td>Master switch.</td></tr><tr><td><code>detectors.trivy.interval_minutes</code></td><td>int</td><td><code>60</code></td><td>Minutes between image scans.</td></tr></tbody></table><p>Scans each running container&#39;s image for HIGH and CRITICAL CVEs; critical CVEs produce high-severity events, the rest medium.</p><h3 id="detectors-containerscan" tabindex="-1">detectors.containerscan <a class="header-anchor" href="#detectors-containerscan" aria-label="Permalink to &quot;detectors.containerscan&quot;">​</a></h3><p>Periodic per-container inspection: processes, log tails, npm dependencies and volume artifacts. Category: <code>scan</code> (critical hash-confirmed hits are reported as <code>malware</code>).</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>detectors.containerscan.enabled</code></td><td>bool</td><td><code>true</code></td><td>Master switch.</td></tr><tr><td><code>detectors.containerscan.interval_minutes</code></td><td>int</td><td><code>3</code></td><td>Minutes between container scan passes.</td></tr><tr><td><code>detectors.containerscan.log_tail_lines</code></td><td>int</td><td><code>1000</code></td><td>Lines of each container&#39;s log tail inspected for indicators.</td></tr><tr><td><code>detectors.containerscan.log_indicators</code></td><td>map</td><td>whatsapp / nezha / miner sets</td><td>Label → substrings matched in log tails: WhatsApp bots (<code>whatsapp-web.js</code>, <code>baileys</code>, <code>yowsup</code>, <code>wa-automate</code>, …), nezha agents (<code>nezha</code>, <code>App is running!</code>), miners (<code>xmrig</code>, <code>cryptonight</code>, <code>stratum+tcp</code>, <code>minexmr</code>, <code>nanopool</code>, <code>minergate</code>, …).</td></tr><tr><td><code>detectors.containerscan.suspicious_words</code></td><td>list</td><td>11 fragments</td><td>Log-tail abuse markers: <code>new job from</code>, <code>noVNC</code>, <code>Downloading fresh proxies...</code>, <code>FAILED TO APPLY MSR MOD</code>, <code>Tor server&#39;s identity key</code>, <code>Stratum - Connected</code>, <code>eth.2miners.com:2020</code>, <code>whatsapp</code>, <code>wa-automate</code>, <code>whatsapp-web.js</code>, <code>baileys</code>.</td></tr><tr><td><code>detectors.containerscan.suspicious_processes</code></td><td>list</td><td><code>xmrig earnfm mcstorm.jar proot destine hashvault</code></td><td>Process names flagged when seen inside a container.</td></tr><tr><td><code>detectors.containerscan.cache_artifacts</code></td><td>list</td><td><code>cpuminer cpuminer-avx2 xmrig</code></td><td>Miner artifacts dropped in cache directories.</td></tr><tr><td><code>detectors.containerscan.whatsapp_deps</code></td><td>list</td><td><code>whatsapp-web.js whatsapp-web-js webwhatsapi yowsup wa-automate baileys</code></td><td>npm dependencies in <code>package.json</code> that indicate a WhatsApp bot.</td></tr><tr><td><code>detectors.containerscan.concurrency</code></td><td>int</td><td><code>5</code></td><td>Containers scanned in parallel.</td></tr><tr><td><code>detectors.containerscan.min_server_jar_bytes</code></td><td>int</td><td><code>5242880</code></td><td>A <code>server.jar</code> smaller than 5 MiB is flagged — real Minecraft jars are never that small; droppers are.</td></tr><tr><td><code>detectors.containerscan.small_volume_mb</code></td><td>float</td><td><code>3.5</code></td><td>Combined with high CPU: a near-empty volume burning CPU is mining.</td></tr><tr><td><code>detectors.containerscan.high_cpu_percent</code></td><td>int</td><td><code>96</code></td><td>CPU component of the tiny-volume heuristic.</td></tr><tr><td><code>detectors.containerscan.high_network_mb</code></td><td>int</td><td><code>4096</code></td><td>Network transfer (MB) component of the high-usage heuristic.</td></tr></tbody></table><p>Severity: suspicious process → high; labeled log indicators → medium or high depending on label; high CPU with tiny volume → high; suspicious log content / high network → medium.</p><h3 id="detectors-volumescan" tabindex="-1">detectors.volumescan <a class="header-anchor" href="#detectors-volumescan" aria-label="Permalink to &quot;detectors.volumescan&quot;">​</a></h3><p>Full filesystem walk of server volumes with sonar-style pattern lists. Category: <code>scan</code>. Also runs on demand from the panel (Scans tab).</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>detectors.volumescan.enabled</code></td><td>bool</td><td><code>true</code></td><td>Master switch.</td></tr><tr><td><code>detectors.volumescan.interval_minutes</code></td><td>int</td><td><code>15</code></td><td>Minutes between scheduled sweeps.</td></tr><tr><td><code>detectors.volumescan.suspicious_names</code></td><td>list</td><td><code>mine.sh working_proxies.txt proxies.txt whatsapp.js wa_bot.js proxy.txt</code></td><td>File names that indicate abuse.</td></tr><tr><td><code>detectors.volumescan.suspicious_extensions</code></td><td>list</td><td><code>.sh</code></td><td>Extensions that warrant a closer look.</td></tr><tr><td><code>detectors.volumescan.mining_patterns</code></td><td>list</td><td>10 substrings</td><td>File-content markers: <code>stratum</code>, <code>cryptonight</code>, <code>proxies...</code>, <code>minexmr.com</code>, <code>herominers</code>, <code>hashvault</code>, <code>xmrig</code>, <code>nanopool.org</code>, <code>ethpool.org</code>, <code>2miners.com</code>.</td></tr><tr><td><code>detectors.volumescan.ignored_files</code></td><td>list</td><td>12 names</td><td>Benign files skipped: <code>velocity.toml</code>, <code>server.jar.old</code>, <code>latest.log</code>, <code>debug.log</code>, <code>error.log</code>, <code>access.log</code>, <code>server.log</code>, <code>usermap.bin</code>, <code>forbidden-players.txt</code>, <code>help.yml</code>, <code>commands.yml</code>, <code>permissions.yml</code>.</td></tr><tr><td><code>detectors.volumescan.ignored_paths</code></td><td>list</td><td>16 prefixes</td><td>Relative path prefixes skipped: <code>proxy.log.0</code>, <code>proxy.log</code>, <code>plugins/.paper-remapped</code>, <code>plugins/CoreProtect/database.db</code>, <code>plugins/PlaceholderAPI/javascripts/example.js</code>, <code>plugins/Geyser-Spigot/locales</code>, <code>plugins/Geyser-Velocity/locales</code>, <code>plugins/Essentials</code>, <code>plugins/ViaVersion/cache</code>, <code>cache</code>, <code>logs</code>, <code>crash-reports</code>, <code>world/playerdata</code>, <code>world/stats</code>, <code>world/advancements</code>, <code>world/region</code>.</td></tr><tr><td><code>detectors.volumescan.ignored_extensions</code></td><td>list</td><td>19 extensions</td><td>Skipped on content scans: <code>.jar</code>, <code>.phar</code>, <code>.rar</code>, <code>.zip</code>, <code>.tar.gz</code>, <code>.7z</code>, <code>.gz</code>, <code>.xz</code>, <code>.bz2</code>, <code>.log</code>, <code>.logs</code>, <code>.txt</code>, <code>.yml</code>, <code>.yaml</code>, <code>.json</code>, <code>.properties</code>, <code>.db</code>, <code>.toml</code>, <code>.mca</code>.</td></tr><tr><td><code>detectors.volumescan.max_jar_size_mb</code></td><td>int</td><td><code>5</code></td><td>Jars above this are treated as real server jars and skipped by undersized-jar checks.</td></tr><tr><td><code>detectors.volumescan.max_file_size_mb</code></td><td>int</td><td><code>64</code></td><td>Files larger than this are not content-scanned (names/extensions still apply).</td></tr><tr><td><code>detectors.volumescan.max_scan_seconds</code></td><td>int</td><td><code>600</code></td><td>Time budget per sweep.</td></tr></tbody></table><hr><h2 id="whitelist" tabindex="-1">whitelist <a class="header-anchor" href="#whitelist" aria-label="Permalink to &quot;whitelist&quot;">​</a></h2><p>Suppression lists applied before rules run.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>whitelist.paths</code></td><td>list</td><td><code>[]</code></td><td>Path prefixes exempt from file-based detection and actions. Matching is prefix-based (<code>/a/b</code> covers <code>/a/b/c</code>).</td></tr><tr><td><code>whitelist.servers</code></td><td>list</td><td><code>[]</code></td><td>Muted server UUIDs. Local actions AND alerts are suppressed for these servers, but events are still recorded with <code>evidence.muted: true</code>.</td></tr></tbody></table><p><strong>When to change.</strong> Mute a server while you investigate a false positive, or whitelist a path that legitimately contains miner-like content (e.g., a security-research egg). Prefer muting one server over disabling a detector fleet-wide.</p><hr><h2 id="rules" tabindex="-1">rules <a class="header-anchor" href="#rules" aria-label="Permalink to &quot;rules&quot;">​</a></h2><p>The enforcement policy: an ordered list mapping (categories, minimum severity) to actions. First match wins; the built-in list ends with a <code>* / low / alert</code> catch-all so every event is at least alerted.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">miners-high</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    categories</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">miner</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    min_severity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">high</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    actions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">alert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">kill_process</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">miners-critical</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    categories</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">miner</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    min_severity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">critical</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    actions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">alert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">stop_container</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">suspend_server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # ...</span></span></code></pre></div><ul><li><strong>Categories:</strong> <code>miner</code>, <code>portscan</code>, <code>ddos</code>, <code>zipbomb</code>, <code>exploit</code>, <code>abuse</code>, <code>malware</code>, <code>fim</code>, <code>vuln</code>, <code>scan</code>, or <code>*</code> for all.</li><li><strong>Severities:</strong> <code>low</code>, <code>medium</code>, <code>high</code>, <code>critical</code> (<code>min_severity</code> is inclusive — <code>high</code> matches high and critical).</li><li><strong>Actions:</strong> <code>alert</code>, <code>quarantine_file</code>, <code>delete_file</code>, <code>kill_process</code>, <code>pause_container</code>, <code>stop_container</code>, <code>suspend_server</code> (the node also accepts <code>log_only</code> and <code>unpause_container</code>).</li></ul><p>The built-in policy (from <code>DefaultRules()</code>):</p><table tabindex="0"><thead><tr><th>Rule</th><th>Categories</th><th>Min severity</th><th>Actions</th></tr></thead><tbody><tr><td>miners-high</td><td>miner</td><td>high</td><td>alert, kill_process</td></tr><tr><td>miners-critical</td><td>miner</td><td>critical</td><td>alert, stop_container, suspend_server</td></tr><tr><td>abuse-high</td><td>abuse</td><td>high</td><td>alert, kill_process</td></tr><tr><td>abuse-critical</td><td>abuse</td><td>critical</td><td>alert, stop_container, suspend_server</td></tr><tr><td>ddos</td><td>ddos</td><td>high</td><td>alert, pause_container, suspend_server</td></tr><tr><td>malware</td><td>malware</td><td>high</td><td>alert, quarantine_file</td></tr><tr><td>exploits</td><td>exploit</td><td>high</td><td>alert, kill_process</td></tr><tr><td>exploits-critical</td><td>exploit</td><td>critical</td><td>alert, stop_container, suspend_server</td></tr><tr><td>zipbombs</td><td>zipbomb</td><td>medium</td><td>alert, quarantine_file</td></tr><tr><td>portscans</td><td>portscan</td><td>medium</td><td>alert</td></tr><tr><td>catch-all</td><td>*</td><td>low</td><td>alert</td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p><code>suspend_server</code> is <strong>not</strong> executed by the node. The node reports the event; the panel suspends via Pterodactyl&#39;s <code>SuspensionService</code> when the event arrives. This is deliberate: suspension is panel-authoritative, idempotent, and works even if the node&#39;s local actions fail. With <code>dry_run: true</code>, no rule action runs on either side.</p></div><p><strong>Common mistakes.</strong> Putting a broad rule (e.g. <code>* / low</code>) above specific ones — first match wins, so the specific rules never fire. Using <code>delete_file</code> where <code>quarantine_file</code> would do — quarantine keeps the file restorable from the Quarantine tab.</p><hr><h2 id="intel" tabindex="-1">intel <a class="header-anchor" href="#intel" aria-label="Permalink to &quot;intel&quot;">​</a></h2><p>Central threat-intel behavior.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>intel.confirm_threshold</code></td><td>int</td><td><code>3</code></td><td>Distinct nodes that must report the same SHA-256 before it flips to <code>confirmed</code> and is distributed fleet-wide. Range 1–50.</td></tr><tr><td><code>intel.external_hashlist_url</code></td><td>string</td><td><code>&quot;&quot;</code> (empty)</td><td>Optional external SHA-256 blocklist feed, pulled on the update interval and merged into the node blocklists. Empty disables.</td></tr><tr><td><code>intel.update_interval_hours</code></td><td>int</td><td><code>24</code></td><td>Hours between external feed pulls and intel bundle refreshes. Range 1–720.</td></tr></tbody></table><p><strong>When to change.</strong> On a small fleet (2–3 nodes), set <code>confirm_threshold: 1</code> or <code>2</code> so intel actually propagates; on large fleets, keep 3+ so one compromised node cannot poison the blocklist. Admin-confirmed and bulk-imported hashes bypass the threshold entirely.</p><hr><h2 id="limits" tabindex="-1">limits <a class="header-anchor" href="#limits" aria-label="Permalink to &quot;limits&quot;">​</a></h2><p>Backpressure guards.</p><table tabindex="0"><thead><tr><th>YAML path</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>limits.max_events_per_minute</code></td><td>int</td><td><code>60</code></td><td>Per-node event rate cap; excess events are dropped (and counted) to protect the panel from a detection storm. Range 1–10000.</td></tr><tr><td><code>limits.spool_max_events</code></td><td>int</td><td><code>10000</code></td><td>Bound on the on-disk event spool used while the panel is unreachable. Oldest events are dropped past the cap. Range 100–1000000.</td></tr></tbody></table><hr><h2 id="panel-side-settings-never-sent-to-nodes" tabindex="-1">Panel-side settings (never sent to nodes) <a class="header-anchor" href="#panel-side-settings-never-sent-to-nodes" aria-label="Permalink to &quot;Panel-side settings (never sent to nodes)&quot;">​</a></h2><h3 id="alert-channels-—-settings-→-alert-channels" tabindex="-1">Alert channels — Settings → Alert Channels <a class="header-anchor" href="#alert-channels-—-settings-→-alert-channels" aria-label="Permalink to &quot;Alert channels — Settings → Alert Channels&quot;">​</a></h3><p>Secrets live only in the panel DB. Each channel has <code>enabled</code>, a destination, and <code>min_severity</code> (the channel only fires for events at or above it).</p><table tabindex="0"><thead><tr><th>Setting</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>alerts.discord.enabled</code></td><td>bool</td><td><code>false</code></td><td>Enable Discord embed alerts.</td></tr><tr><td><code>alerts.discord.webhook_url</code></td><td>string</td><td><code>&quot;&quot;</code></td><td>Discord webhook URL; validated to match \`https://(canary.</td></tr><tr><td><code>alerts.discord.min_severity</code></td><td>enum</td><td><code>high</code></td><td>Minimum severity posted to Discord.</td></tr><tr><td><code>alerts.webhook.enabled</code></td><td>bool</td><td><code>false</code></td><td>Enable the generic JSON webhook.</td></tr><tr><td><code>alerts.webhook.url</code></td><td>string</td><td><code>&quot;&quot;</code></td><td>Any <code>http(s)://</code> endpoint receiving the event payload as JSON.</td></tr><tr><td><code>alerts.webhook.min_severity</code></td><td>enum</td><td><code>high</code></td><td>Minimum severity posted to the webhook.</td></tr><tr><td><code>alerts.smtp.enabled</code></td><td>bool</td><td><code>false</code></td><td>Enable email alerts via the panel&#39;s configured mailer.</td></tr><tr><td><code>alerts.smtp.recipients</code></td><td>string</td><td><code>&quot;&quot;</code></td><td>Space/comma-separated recipient addresses; each is email-validated.</td></tr><tr><td><code>alerts.smtp.min_severity</code></td><td>enum</td><td><code>critical</code></td><td>Minimum severity emailed. Defaults to critical to keep inboxes quiet.</td></tr></tbody></table><p>Alert dispatch is subject to per-channel cooldown and the global rate limit — see <a href="./../user-guide/webhooks">Webhooks &amp; Alerts</a>.</p><h3 id="per-node-overrides-—-settings-→-node-overrides" tabindex="-1">Per-node overrides — Settings → Node Overrides <a class="header-anchor" href="#per-node-overrides-—-settings-→-node-overrides" aria-label="Permalink to &quot;Per-node overrides — Settings → Node Overrides&quot;">​</a></h3><p>A JSON tree stored per node as <code>node_config_override_&lt;node_id&gt;</code>, deep-merged <strong>over</strong> the global tree when the panel builds that node&#39;s effective config. Scalars and lists are replaced wholesale; associative maps merge recursively. Saving an override bumps <code>config_version</code> and pushes to that node like any other change.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;detectors&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;miner&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;cpu_threshold&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">95</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;trivy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;enabled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;general&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;log_level&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;debug&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Use overrides for one noisy node (raise thresholds), one beefy node (enable trivy/yara sweeps), or debugging a single agent — not as your primary config mechanism. Deleting the override reverts the node to the global tree on the next push.</p><h3 id="other-panel-settings" tabindex="-1">Other panel settings <a class="header-anchor" href="#other-panel-settings" aria-label="Permalink to &quot;Other panel settings&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Setting</th><th>Where</th><th>What it does</th></tr></thead><tbody><tr><td><code>config_version</code></td><td>internal</td><td>Monotonic counter, bumped on every config/intel save; how nodes learn they must re-apply.</td></tr><tr><td><code>yara_rules</code></td><td>Settings → Detectors</td><td>The YARA rule bundle text (max 200 KB), distributed to nodes with <code>yara_version</code>.</td></tr><tr><td><code>sentinel.offline_after_seconds</code></td><td><code>config/sentinel.php</code></td><td>Seconds without a heartbeat before housekeeping marks a node offline. Default <code>120</code>.</td></tr><tr><td><code>sentinel.event_retention_days</code></td><td><code>config/sentinel.php</code></td><td>Days of events kept before <code>sentinel:housekeeping</code> prunes them. Default <code>30</code>.</td></tr></tbody></table><hr><h2 id="node-local-etc-sentinel-config-yaml" tabindex="-1">Node-local <code>/etc/sentinel/config.yaml</code> <a class="header-anchor" href="#node-local-etc-sentinel-config-yaml" aria-label="Permalink to &quot;Node-local \`/etc/sentinel/config.yaml\`&quot;">​</a></h2><p>Written by <code>install.sh</code>; mode <code>0600</code>. This is the <strong>only</strong> file you edit by hand, and only for connectivity changes.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">api</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  listen</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0.0.0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # bind address for the agent API</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  port</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8481</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # bind port (panel calls back here)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">panel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">https://panel.example.com</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # panel base URL</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  token_file</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/etc/sentinel/token</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # 64-hex pairing token (0600)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">state_dir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/var/lib/sentinel</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # spool, quarantine, intel, persisted config</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">docker_socket</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/var/run/docker.sock</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">log_level</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">info</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Key</th><th>What it does</th><th>When to change</th></tr></thead><tbody><tr><td><code>api.listen</code> / <code>api.port</code></td><td>Where the agent&#39;s HTTP API binds.</td><td>Port conflicts; binding to a specific interface. Must stay reachable from the panel for config push.</td></tr><tr><td><code>panel.url</code></td><td>Panel base URL the agent registers/heartbeats to.</td><td>Panel domain change. Trailing slash is stripped by the installer.</td></tr><tr><td><code>panel.token_file</code></td><td>Path to the pairing token.</td><td>Never — re-run <code>install.sh</code> to re-pair instead.</td></tr><tr><td><code>state_dir</code></td><td>Runtime state root.</td><td>Moving state to a bigger disk.</td></tr><tr><td><code>docker_socket</code></td><td>Docker socket path.</td><td>Non-standard docker setups.</td></tr><tr><td><code>log_level</code></td><td>Local bootstrap log level (the pushed config&#39;s <code>general.log_level</code> takes over once applied).</td><td>Early-boot debugging.</td></tr></tbody></table><p>After editing, restart the agent: <code>systemctl restart sentinel-node</code>.</p>`,106)])])}const g=t(a,[["render",i]]);export{u as __pageData,g as default};
