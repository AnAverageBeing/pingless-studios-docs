import{_ as i,o as e,c as t,a2 as a}from"./chunks/framework.CZqXuAiL.js";const c=JSON.parse('{"title":"Transparent Client IP — OpenShield-L7","description":"IP_TRANSPARENT deep dive — how OpenShield-L7 sources upstream connections from the real client IP, the required sysctls/routes for both topologies, limits, and security notes.","frontmatter":{"title":"Transparent Client IP — OpenShield-L7","description":"IP_TRANSPARENT deep dive — how OpenShield-L7 sources upstream connections from the real client IP, the required sysctls/routes for both topologies, limits, and security notes.","outline":"deep"},"headers":[],"relativePath":"openshield-l7/architecture/transparent-ip.md","filePath":"openshield-l7/architecture/transparent-ip.md","lastUpdated":1786524249000}'),n={name:"openshield-l7/architecture/transparent-ip.md"};function o(l,s,r,h,p,d){return e(),t("div",null,[...s[0]||(s[0]=[a(`<h1 id="transparent-client-ip" tabindex="-1">Transparent Client IP <a class="header-anchor" href="#transparent-client-ip" aria-label="Permalink to &quot;Transparent Client IP&quot;">​</a></h1><p>Normally a reverse proxy hides the client: the origin&#39;s peer is the proxy, and the real client IP travels in <code>X-Forwarded-For</code> or a PROXY protocol header — both of which the origin must be configured to read.</p><p>With <code>client_ip.transparent: true</code> on a site, OpenShield-L7 instead sources the <strong>upstream TCP connection from the real client IP</strong>: the proxy binds its origin-facing socket with the Linux <code>IP_TRANSPARENT</code> option and the client IP as local address before connecting. The origin&#39;s own socket table then shows the connection as coming from the client directly — no headers, no PROXY protocol, <strong>zero origin configuration</strong>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>normal mode:       client ──▶ openshield ──▶ origin      origin peer = openshield IP</span></span>
<span class="line"><span>transparent mode:  client ──▶ openshield ──▶ origin      origin peer = client IP</span></span>
<span class="line"><span>                   (e.g. with a CDN: cf ──▶ openshield ──▶ origin, but</span></span>
<span class="line"><span>                    the origin sees cf&#39;s downstream client, not cf, not us)</span></span></code></pre></div><p>The spoofed source is the <strong>resolved client IP</strong> — the trusted-proxy-aware one (see <code>client_ip.trusted_proxies</code> in the <a href="./../configuration/reference#client_ip">Configuration Reference</a>). With a CDN in front and its ranges trusted, the origin sees the true end client; without trusted proxies, it sees the direct peer. The same resolved IP feeds the PROXY-protocol source address on <code>proxy_protocol</code> sites.</p><p>When transparent mode is on, <code>forward_mode</code> and <code>proxy_protocol</code> are redundant for IP visibility — <code>forward_mode: none</code> is the clean combination.</p><div class="danger custom-block"><p class="custom-block-title">DANGER</p><p>This is deliberately-spoofed-source networking. It only works when the network path cooperates, and it needs real privileges. Read this whole page before enabling it.</p></div><hr><h2 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h2><p>For each upstream connection of a transparent site, the proxy:</p><ol><li>resolves the client IP as usual;</li><li>creates the upstream socket, sets <code>IP_TRANSPARENT</code> on it, and <code>bind()</code>s it to <code>(client_ip, 0)</code>;</li><li><code>connect()</code>s to the origin.</li></ol><p>The origin answers to the client IP — which is <strong>not</strong> a local address of the proxy box — so the reply packets must find their way back to the proxy&#39;s socket. That is the entire difficulty, and both supported topologies below are just two ways of arranging it.</p><p>Privileges are checked <strong>once at startup</strong> (root or <code>CAP_NET_ADMIN</code>). If they are missing, the proxy logs loudly and <strong>falls back to normal egress</strong> — the site keeps working, the origin just sees the proxy IP again (graceful degrade).</p><hr><h2 id="requirements" tabindex="-1">Requirements <a class="header-anchor" href="#requirements" aria-label="Permalink to &quot;Requirements&quot;">​</a></h2><ul><li>Linux, running as <strong>root</strong> or with <strong><code>CAP_NET_ADMIN</code></strong> (see the <a href="./../getting-started/installation#transparent-mode-variant">systemd variants</a>).</li><li>The proxy box must <strong>receive the origin&#39;s replies</strong> to the client IP. One of the two topologies below.</li><li><code>rp_filter</code> must not drop the spoofed traffic.</li><li>No NAT on the path that would rewrite the source (masquerading kills transparency).</li><li>A network you control. See <a href="#where-it-cannot-work">Where it cannot work</a>.</li></ul><p>Limits:</p><ul><li><strong>IPv4-only</strong> — <code>IP_TRANSPARENT</code> is only set on IPv4 sockets (a socket2 limitation). IPv6 clients degrade to normal egress with a log line.</li><li><strong>Return-path requirement</strong> — the origin&#39;s replies must transit the proxy box. If they can route around it, the handshake never completes.</li></ul><hr><h2 id="topology-a-—-gateway-mode-origin-routes-via-this-box" tabindex="-1">Topology A — gateway mode (origin routes via this box) <a class="header-anchor" href="#topology-a-—-gateway-mode-origin-routes-via-this-box" aria-label="Permalink to &quot;Topology A — gateway mode (origin routes via this box)&quot;">​</a></h2><p>The proxy box is the origin&#39;s <strong>default gateway</strong> (same L2 segment, or the origin is otherwise routed through it). Replies to client IPs naturally flow back through the box; policy routing then delivers them to the local transparent sockets instead of forwarding them on.</p><p>Packet walk: SYN <code>client → origin</code> leaves the box with <code>src=client</code> → origin replies <code>origin → client</code> → reply arrives back at the box because it is the origin&#39;s gateway → policy routing marks it &quot;local&quot; → socket lookup matches the proxy&#39;s <code>IP_TRANSPARENT</code> socket → proxied back to the real client.</p><p>Setup on the OpenShield box:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Forward packets (the box is a gateway).</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sysctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -w</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> net.ipv4.ip_forward=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Packets with foreign source/destination addresses are not martians here.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sysctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -w</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> net.ipv4.conf.all.rp_filter=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sysctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -w</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> net.ipv4.conf.default.rp_filter=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># (or set rp_filter=2 &quot;loose&quot; per-interface instead of 0)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Deliver reply packets (origin -&gt; client) to local sockets:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># mark them, then route the marked ones to loopback.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rule</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fwmark</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0x1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lookup</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 100</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pref</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 100</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> local</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 0.0.0.0/0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> table</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 100</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># eth1 = the interface facing the origin; 10.0.0.5/8080 = the origin.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iptables</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mangle</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -A</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> PREROUTING</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> eth1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tcp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10.0.0.5</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --sport</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8080</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> MARK</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --set-xmark</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0x1</span></span></code></pre></div><p>And on the origin host: its default route via the OpenShield box (e.g. <code>ip route replace default via &lt;box-ip&gt;</code>), or an equivalent DHCP / static-route setup.</p><p>Notes:</p><ul><li>Tighten the MARK rule with <code>-i</code>/<code>-s</code>/<code>--sport</code> so unrelated traffic keeps its normal path. One rule per origin (or match a subnet).</li><li>Do <strong>not</strong> MASQUERADE/SNAT this traffic — any source rewrite destroys the transparency.</li><li>If the box only proxies and does not otherwise route, <code>ip_forward=1</code> is still required so the box doesn&#39;t reject the transit packets.</li></ul><hr><h2 id="topology-b-—-local-origin-the-origin-is-a-process-on-this-box" tabindex="-1">Topology B — local origin (the origin is a process on this box) <a class="header-anchor" href="#topology-b-—-local-origin-the-origin-is-a-process-on-this-box" aria-label="Permalink to &quot;Topology B — local origin (the origin is a process on this box)&quot;">​</a></h2><p>The origin listens on <code>127.0.0.1:8080</code> (or another address of the same host). Here the origin&#39;s replies are <strong>locally generated</strong>, so they never hit PREROUTING — they go through OUTPUT routing, which would happily send <code>origin → client</code> packets out the default gateway to die. Mark them in OUTPUT and loop them back instead.</p><p>Setup on the OpenShield box (origin at <code>127.0.0.1:8080</code>):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Loopback-hosted origins: allow 127/8 traffic to be policy-routed</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># without tripping the kernel&#39;s martian checks.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sysctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -w</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> net.ipv4.conf.all.route_localnet=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Same local-delivery trick as gateway mode.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rule</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fwmark</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0x1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lookup</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 100</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pref</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 100</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> local</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 0.0.0.0/0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> table</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 100</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Replies are locally generated -&gt; OUTPUT chain, matched by source port.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iptables</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mangle</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -A</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> OUTPUT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tcp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 127.0.0.1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --sport</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8080</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> MARK</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --set-xmark</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0x1</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># The spoofed SYN arrives on lo with a foreign source: don&#39;t let</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># rp_filter martian-drop it.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sysctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -w</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> net.ipv4.conf.all.rp_filter=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sysctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -w</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> net.ipv4.conf.lo.rp_filter=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span></span></code></pre></div><p>If the origin binds a non-loopback local address (e.g. <code>10.0.0.5</code> on eth0), drop <code>route_localnet</code> and match <code>-s 10.0.0.5</code> instead.</p><hr><h2 id="systemd-companion-unit" tabindex="-1">systemd companion unit <a class="header-anchor" href="#systemd-companion-unit" aria-label="Permalink to &quot;systemd companion unit&quot;">​</a></h2><p>The routing side is <strong>not</strong> done by the proxy — install it once via network configuration or a companion oneshot unit (Topology B example):</p><div class="language-ini vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ini</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># /etc/systemd/system/openshield-l7-transparent.service</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">[Unit]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Description</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=Policy routing for OpenShield-L7 transparent mode</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Before</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=openshield-l7.service</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">After</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=network-pre.target</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Wants</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=network-pre.target</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">[Service]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=oneshot</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">RemainAfterExit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=yes</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Topology B (local origin 127.0.0.1:8080) example:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ExecStart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=/sbin/sysctl -w </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">net.ipv4.conf.all.route_localnet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ExecStart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=/sbin/sysctl -w </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">net.ipv4.conf.all.rp_filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=0</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ExecStart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=/sbin/sysctl -w </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">net.ipv4.conf.lo.rp_filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=0</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ExecStart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=/sbin/ip rule add fwmark 0x1 lookup 100 pref 100</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ExecStart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=/sbin/ip route add local 0.0.0.0/0 dev lo table 100</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ExecStart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=/sbin/iptables -t mangle -A OUTPUT -p tcp -s 127.0.0.1 --sport 8080 -j MARK --set-xmark 0x1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ExecStop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=/sbin/ip rule del fwmark 0x1 lookup 100 pref 100</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ExecStop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=/sbin/ip route del local 0.0.0.0/0 dev lo table 100</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ExecStop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=/sbin/iptables -t mangle -D OUTPUT -p tcp -s 127.0.0.1 --sport 8080 -j MARK --set-xmark 0x1</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">[Install]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WantedBy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=multi-user.target</span></span></code></pre></div><p>The main unit needs the <a href="./../getting-started/installation#transparent-mode-variant">transparent-mode variant</a> (<code>CAP_NET_ADMIN</code>, no <code>NoNewPrivileges</code>).</p><hr><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>Transparent mode degrades gracefully only for <strong>missing privileges</strong>. A network path that blackholes the replies is <em>not</em> auto-detected — the site just serves 502s. Verify after every setup change:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 1. Privileges were detected (no &quot;transparent&quot; warning in the log).</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 2. Send a request through the proxy and watch the ORIGIN&#39;s socket table:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#    you should see the connection sourced from your test client IP.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ss</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -tnp</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8080</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ESTAB 0 0 10.0.0.5:8080 203.0.113.9:51234 ...</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 3. On the proxy box, confirm replies are coming back and being marked:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tcpdump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -ni</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> any</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;tcp port 8080 and host 203.0.113.9&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iptables</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mangle</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -L</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> OUTPUT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -v</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -n</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # packet counters on the MARK rule</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 4. Functional check through the proxy:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Host: example.com&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">proxy-i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">p</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/</span></span></code></pre></div><hr><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Symptom</th><th>Likely cause</th><th>Fix</th></tr></thead><tbody><tr><td>Startup log: transparent unsupported, falling back</td><td>Not root / no <code>CAP_NET_ADMIN</code></td><td>Use the <code>AmbientCapabilities</code> systemd variant or run as root.</td></tr><tr><td>502s, origin <code>ss</code> shows SYN_RECV from client IPs</td><td>Replies never come back</td><td>Wrong topology: the origin must route via this box (A) or be local (B). Check <code>ip rule</code>/<code>ip route ... table 100</code> and the MARK rule counters.</td></tr><tr><td>Works from some clients, not others</td><td>rp_filter dropping asymmetric-looking packets</td><td><code>rp_filter=0</code> (or <code>2</code>) on all involved interfaces, incl. <code>lo</code> for local origins.</td></tr><tr><td>Local origin: nothing works at all</td><td>Martian drops on 127/8 traffic</td><td><code>sysctl net.ipv4.conf.all.route_localnet=1</code>.</td></tr><tr><td><code>dmesg</code>/logs show &quot;martian source&quot; messages</td><td>Strict reverse-path filtering</td><td>Same as above; find the interface in the log and relax its <code>rp_filter</code>.</td></tr><tr><td>Connections establish, then stall/reset</td><td>Conntrack table full, or a conntrack/NAT helper mangling the flow</td><td>Raise <code>net.netfilter.nf_conntrack_max</code>; ensure no MASQUERADE/SNAT matches this traffic; check <code>conntrack -L | grep &lt;client-ip&gt;</code>.</td></tr><tr><td>Origin sees the proxy IP anyway</td><td>Transparent silently off (privilege fallback) or config not reloaded</td><td>Check startup log; confirm the site file has <code>client_ip.transparent: true</code>; <code>POST /api/v1/reload</code>.</td></tr></tbody></table><p>Conntrack notes: with connection tracking enabled, both directions of the spoofed flow are tracked like any other flow — fine — but under flood conditions the conntrack table becomes the bottleneck (<code>nf_conntrack: table full, dropping packet</code>). Size <code>net.netfilter.nf_conntrack_max</code> for your expected concurrent flows, or <code>NOTRACK</code> the proxy↔origin traffic if you don&#39;t otherwise need it.</p><h3 id="where-it-cannot-work" tabindex="-1">Where it cannot work <a class="header-anchor" href="#where-it-cannot-work" aria-label="Permalink to &quot;Where it cannot work&quot;">​</a></h3><p>Transparent mode requires the spoofed source IP to survive the network path in both directions. It <strong>cannot</strong> work when:</p><ul><li><strong>Cloud NAT / filtered egress</strong>: AWS/GCP/Azure and most VPS providers drop packets whose source IP isn&#39;t assigned to your NIC (source/destination checks, hypervisor anti-spoofing). A <em>remote</em> origin behind such infrastructure will never see the client IP. (Local origins on the same cloud VM — Topology B — do work, since nothing spoofed leaves the box.)</li><li><strong>The origin&#39;s replies don&#39;t transit this box</strong> (origin on the public internet with its own gateway): the SYN-ACK goes straight to the real client, whose stack RSTs a connection it never opened. Handshake timeouts on the proxy, <code>SYN_RECV</code> on the origin.</li><li><strong>Carrier-grade NAT or upstream BCP38 filtering</strong> between you and the origin: spoofed egress is dropped by design.</li></ul><p>Detection recipe: <code>tcpdump</code> on the proxy box for SYNs leaving with <code>src=client</code>, no SYN-ACKs returning → the path is filtering you. There is no automatic fallback for this case (the failure looks like an origin timeout); disable <code>transparent</code> for that site and use <code>forward_mode</code>/<code>proxy_protocol</code> instead.</p><hr><h2 id="security-notes" tabindex="-1">Security notes <a class="header-anchor" href="#security-notes" aria-label="Permalink to &quot;Security notes&quot;">​</a></h2><ul><li>You are deliberately spoofing source addresses. Any <strong>egress filtering you operate</strong> (uRPF on your routers, provider ACLs) must permit it for this host — and should permit it <em>only</em> for this host.</li><li>Enable transparent mode only when you control the full network path between proxy and origin. On shared or filtered infrastructure it will either fail closed (timeouts) or train you to ignore spoofing alarms.</li><li>The proxy host becomes the one machine allowed to originate arbitrary-source traffic: keep the admin API on loopback, keep tokens out of dashboards, and patch the box.</li><li>Transparent mode does not replace origin lockdown. The origin port must still not be publicly reachable, or attackers bypass the proxy entirely — see <a href="./../getting-started/installation#verify-the-install">origin lockdown</a>.</li></ul>`,53)])])}const g=i(n,[["render",o]]);export{c as __pageData,g as default};
