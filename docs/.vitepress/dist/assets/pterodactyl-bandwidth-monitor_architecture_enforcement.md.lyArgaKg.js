import{_ as s,o as i,c as t,a2 as a}from"./chunks/framework.CZqXuAiL.js";const k=JSON.parse(`{"title":"Enforcement","description":"How Bandwidth Monitor enforces limits on a Wings node — tc HTB egress shaping and ingress policing on container veths, the quota engine's calendar periods and exceed transitions, restarts and rollovers, and the on-disk file layout.","frontmatter":{"title":"Enforcement","description":"How Bandwidth Monitor enforces limits on a Wings node — tc HTB egress shaping and ingress policing on container veths, the quota engine's calendar periods and exceed transitions, restarts and rollovers, and the on-disk file layout."},"headers":[],"relativePath":"pterodactyl-bandwidth-monitor/architecture/enforcement.md","filePath":"pterodactyl-bandwidth-monitor/architecture/enforcement.md","lastUpdated":1785221274000}`),n={name:"pterodactyl-bandwidth-monitor/architecture/enforcement.md"};function r(d,e,h,l,o,p){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="enforcement" tabindex="-1">Enforcement <a class="header-anchor" href="#enforcement" aria-label="Permalink to &quot;Enforcement&quot;">​</a></h1><p>Everything in this page happens on the Wings node, in the <code>bandwidth-noded</code> agent. Enforcement is kernel-level: the agent programs the Linux traffic-control subsystem, and the kernel does the shaping — there is no userspace proxy in the data path. This page covers the exact <code>tc</code> rules, the quota engine&#39;s state transitions, and what lives where on disk.</p><p>What this looks like in practice was verified live on a real panel + Wings node: a 10 Mbps cap held throughput at ~9.5 Mbps, a 1 GiB quota exceed throttled the server to its 5 Mbps throttle speeds with <code>quota_exceeded</code> + <code>throttled</code> events visible in the panel, a weekly quota exceed with the <code>suspend</code> action produced a real Pterodactyl suspension, and an admin unthrottle restored full speed immediately.</p><hr><h2 id="tc-deep-dive" tabindex="-1">tc deep dive <a class="header-anchor" href="#tc-deep-dive" aria-label="Permalink to &quot;tc deep dive&quot;">​</a></h2><p>The agent manages rules on the <strong>host-side veth</strong> of each server container. The container itself sees nothing but a constrained pipe — tenants cannot inspect, alter, or evade the rules from inside.</p><p>All <code>tc</code> operations are serialized through a single mutex, and every apply starts by deleting any existing root and ingress qdiscs on the interface, so rules are always rebuilt from a clean slate. A speed of <code>0</code> means &quot;no rule&quot; for that direction; when both directions are 0, all rules are removed from the interface.</p><h3 id="egress-server-tx-—-htb-hard-cap" tabindex="-1">Egress (server TX) — HTB hard cap <a class="header-anchor" href="#egress-server-tx-—-htb-hard-cap" aria-label="Permalink to &quot;Egress (server TX) — HTB hard cap&quot;">​</a></h3><p>When <code>tx_speed_mbps &gt; 0</code>, the agent builds an HTB (Hierarchical Token Bucket) tree on the veth:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> qdisc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">vet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">h</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> root</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> handle</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 1:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> htb</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> default</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> class</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">vet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">h</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> parent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 1:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> classid</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 1:1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> htb</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    rate</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">kbi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ceil</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">kbi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">vet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">h</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> parent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 1:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> protocol</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   prio</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> u32</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> match</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  dst</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 0.0.0.0/0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> flowid</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 1:1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">vet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">h</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> parent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 1:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> protocol</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ipv6</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> prio</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> u32</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> match</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ip6</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dst</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ::/0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      flowid</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 1:1</span></span></code></pre></div><p>Anatomy:</p><ul><li><strong><code>rate</code> = <code>ceil</code>.</strong> The ceiling is deliberately set equal to the rate. A higher <code>ceil</code> would let the class borrow bandwidth and burst past the cap; equal values make it a hard cap.</li><li><strong>Catch-all filters for IPv4 and IPv6.</strong> <code>default 1</code> plus both <code>u32</code> match-all filters route every packet — regardless of protocol — into the single class.</li><li><strong>Unit conversion.</strong> Mbps × 1000 = kbit (tc speaks kilobits). Rates below 8 kbit are clamped to 8 kbit, the minimum tc accepts.</li></ul><h3 id="ingress-server-rx-—-policing" tabindex="-1">Ingress (server RX) — policing <a class="header-anchor" href="#ingress-server-rx-—-policing" aria-label="Permalink to &quot;Ingress (server RX) — policing&quot;">​</a></h3><p>Shaping inbound traffic is not directly possible (the packets have already arrived), so RX caps use the <strong>ingress qdisc with a policer</strong> — excess packets are dropped, which pushes back on TCP congestion control:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> qdisc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">vet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">h</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> handle</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ffff:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ingress</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">vet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">h</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> parent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ffff:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> protocol</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> all</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> prio</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 50</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> u32</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> match</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> u32</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    police</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rate</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">kbi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> burst</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">kbi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> drop</span></span></code></pre></div><ul><li><code>rate</code> is the RX cap in kbit.</li><li><code>burst</code> is <code>rate / 10</code>, clamped to a minimum of 16 kbit — enough headroom for normal TCP behavior without letting sustained throughput exceed the cap.</li><li>The policer matches all protocols in one filter (<code>protocol all</code>, match-all <code>u32</code>).</li></ul><h3 id="veth-lifecycle" tabindex="-1">veth lifecycle <a class="header-anchor" href="#veth-lifecycle" aria-label="Permalink to &quot;veth lifecycle&quot;">​</a></h3><ul><li><strong>Detection.</strong> The agent resolves a container&#39;s veth by peer ifindex matching: it reads the container&#39;s <code>eth0</code> <code>iflink</code>, then finds the host interface with that <code>ifindex</code>. If detection fails it returns an empty string — <strong>never a guess</strong> — because shaping the wrong interface would throttle the wrong customer. Servers without a resolved veth are skipped by enforcement until one is found.</li><li><strong>Apply.</strong> Rules are (re)applied whenever effective speeds change: a limits push/pull, a quota transition, a period reset, or <code>bandwidth-node reapply</code>.</li><li><strong>Boot.</strong> On startup the agent restores every persisted server from SQLite and re-applies tc rules for all running containers — enforcement survives agent restarts and node reboots.</li><li><strong>Stop/remove.</strong> When a container stops or is removed (from a Docker event or the periodic scan), its rules are deleted and its monitor snapshot forgotten. Servers forgotten by cleanup (unseen for 72h by default) get the same treatment.</li><li><strong>Repair.</strong> The manager can reconcile drift: rules whose server or interface no longer exists are removed (<code>RepairRules</code>), and <code>VerifyAll</code> reports interfaces that lost their qdiscs.</li><li><strong>Shutdown.</strong> Graceful daemon stop removes every managed rule (<code>RemoveAll</code>) — after all goroutines have drained, so nothing can re-add a rule mid-teardown.</li><li><strong>Uninstall.</strong> <code>uninstall.sh</code> independently sweeps every <code>veth*</code> interface and deletes root + ingress qdiscs wherever an <code>htb</code> or <code>ingress</code> qdisc is present. Docker itself never attaches qdiscs to veths, so this only ever touches rules the agent created.</li></ul><hr><h2 id="the-quota-engine" tabindex="-1">The quota engine <a class="header-anchor" href="#the-quota-engine" aria-label="Permalink to &quot;The quota engine&quot;">​</a></h2><p>Quotas are cumulative byte counters, tracked <strong>per direction and per period</strong> — six counters per server: <code>rx/tx × day/week/month</code>. Every 5-second poll cycle adds the veth deltas to all six counters (and the lifetime totals) and persists them to SQLite.</p><h3 id="calendar-periods" tabindex="-1">Calendar periods <a class="header-anchor" href="#calendar-periods" aria-label="Permalink to &quot;Calendar periods&quot;">​</a></h3><p>Periods are anchored to the calendar in the <strong>effective timezone</strong> — the agent&#39;s configured <code>timezone</code> (default <code>UTC</code>), overridable by the panel through the <code>timezone</code> field of the limits payload:</p><table tabindex="0"><thead><tr><th>Period</th><th>Starts at</th></tr></thead><tbody><tr><td>Day</td><td>Local midnight</td></tr><tr><td>Week</td><td>Monday 00:00 local</td></tr><tr><td>Month</td><td>The 1st, 00:00 local</td></tr></tbody></table><h3 id="rollover" tabindex="-1">Rollover <a class="header-anchor" href="#rollover" aria-label="Permalink to &quot;Rollover&quot;">​</a></h3><p>The tracked start of each period is persisted in the SQLite <code>config</code> table (<code>period_start_day/week/month</code>). Every poll cycle, and once at startup, the agent compares the persisted start against the expected calendar start:</p><ul><li><strong>Boundary passed</strong> → that period&#39;s counters are zeroed for all servers, the new start is persisted, and affected servers are re-evaluated (see restore below).</li><li><strong>Rollover after downtime</strong> — because starts are persisted, an agent that was down across midnight/Monday/the 1st rolls the period over on its first poll after boot. Missed boundaries are never skipped; the comparison is against the calendar, not a timer.</li><li><strong>Backward movement is not a rollover.</strong> If the expected start moved <em>backwards</em> (timezone change or a backward clock jump), counters are preserved and only the tracked start is updated — accumulated usage is never zeroed spuriously.</li></ul><h3 id="exceed-transitions" tabindex="-1">Exceed transitions <a class="header-anchor" href="#exceed-transitions" aria-label="Permalink to &quot;Exceed transitions&quot;">​</a></h3><p>Each poll evaluates every <code>direction_period</code> combo whose quota is set (&gt; 0): exceeded when <code>used &gt;= quota</code>. Combos currently overridden by an admin unthrottle are skipped. What happens depends on the server&#39;s <code>exceed_action</code>:</p><table tabindex="0"><thead><tr><th><code>exceed_action</code></th><th>On exceed</th></tr></thead><tbody><tr><td><code>throttle</code></td><td>Re-apply tc at the throttle speeds, per exceeded direction: an exceeded <code>rx_*</code> combo drops RX to <code>throttle_rx_mbps</code>, an exceeded <code>tx_*</code> combo drops TX to <code>throttle_tx_mbps</code> (values ≤ 0 become 1 Mbps). The non-exceeded direction keeps its configured cap</td></tr><tr><td><code>suspend</code></td><td>Re-apply tc at a flat <strong>1 Mbps both directions</strong>, then POST <code>/api/bandwidth/node/suspend</code> to the panel, which suspends the Pterodactyl server and records a <code>suspended</code> event</td></tr><tr><td><code>none</code></td><td>No tc change — event only</td></tr></tbody></table><p>Every newly exceeded combo emits a <code>quota_exceeded</code> event (with direction, period, and used/quota bytes); the enforcement transition emits a matching <code>throttled</code> event. Events are queued in SQLite and delivered to the panel with retry, so a panel outage delays — but never loses — the audit trail.</p><p>The suspend callback is treated as must-deliver: the agent retries immediately (5 attempts, backoff doubling from 2s), then a scheduler job retries every minute until the panel confirms. The panel side is idempotent — repeat callbacks stay silent.</p><h3 id="restore-on-period-reset" tabindex="-1">Restore on period reset <a class="header-anchor" href="#restore-on-period-reset" aria-label="Permalink to &quot;Restore on period reset&quot;">​</a></h3><p>When a period rolls over, its counters are zeroed and any admin unthrottle overrides for that period are dropped. Servers whose exceeded combos all belonged to the rolled-over period have their normal speeds re-applied and a <code>restored</code> event is emitted. Servers still exceeded under a <em>longer</em> period (e.g. the day reset but the month quota is still blown) stay throttled — enforcement is re-evaluated against the full combo set, not blindly cleared.</p><h3 id="admin-unthrottle" tabindex="-1">Admin unthrottle <a class="header-anchor" href="#admin-unthrottle" aria-label="Permalink to &quot;Admin unthrottle&quot;">​</a></h3><p><code>POST /api/v1/servers/{uuid}/unthrottle</code> (from the panel&#39;s Servers page, or the CLI) records each currently-exceeded combo as an <strong>override</strong>: the combo is excluded from quota evaluation, enforcement is re-run (which restores normal speeds when nothing un-overridden remains), and the override list is persisted so it survives agent restarts. Overrides are dropped when their period resets — an unthrottle buys time until the end of the current day/week/month, not forever.</p><h3 id="limits-removal" tabindex="-1">Limits removal <a class="header-anchor" href="#limits-removal" aria-label="Permalink to &quot;Limits removal&quot;">​</a></h3><p>A <code>PUT /limits</code> payload is a full replace: servers absent from it lose their limits, enforcement flags, and tc rules (with a <code>restored</code> event if they were restricted). Setting <code>enabled: false</code> similarly means no caps and no quota enforcement for that server.</p><hr><h2 id="file-layout-on-a-node" tabindex="-1">File layout on a node <a class="header-anchor" href="#file-layout-on-a-node" aria-label="Permalink to &quot;File layout on a node&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Path</th><th>Contents</th></tr></thead><tbody><tr><td><code>/etc/bandwidth-node/config.yaml</code></td><td>Agent configuration (YAML, <code>0600</code>) — listen address/port, poll intervals, retention, panel URL, timezone</td></tr><tr><td><code>/etc/bandwidth-node/token</code></td><td>The 64-hex pairing token (<code>0600</code>), shared with the panel; used for both API auth directions</td></tr><tr><td><code>/var/lib/bandwidth-node/bandwidth-node.db</code></td><td>SQLite state (WAL mode): servers + counters + enforcement flags, hourly history, outbound event queue, period starts, config version</td></tr><tr><td><code>/var/log/bandwidth-node/bandwidth-node.log</code></td><td>Rotating log (JSON or text; 100 MB max size, 10 backups, 30 days, compressed)</td></tr><tr><td><code>/var/run/bandwidth-node.sock</code></td><td>Unix socket for the <code>bandwidth-node</code> CLI ↔ daemon protocol</td></tr><tr><td><code>/usr/local/bin/bandwidth-noded</code></td><td>The daemon binary (runs under systemd)</td></tr><tr><td><code>/usr/local/bin/bandwidth-node</code></td><td>The CLI (<code>status</code>, <code>list</code>, <code>limits</code>, <code>unthrottle &lt;uuid&gt;</code>, <code>reapply</code>, <code>doctor</code>, <code>start/stop/restart/logs</code>)</td></tr><tr><td><code>/etc/systemd/system/bandwidth-node.service</code></td><td>The systemd unit</td></tr></tbody></table><h3 id="operational-commands" tabindex="-1">Operational commands <a class="header-anchor" href="#operational-commands" aria-label="Permalink to &quot;Operational commands&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bandwidth-node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # daemon + panel sync status, tc rule count, config_version</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bandwidth-node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                   # live per-server counters</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bandwidth-node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> limits</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # currently enforced limits</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bandwidth-node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> unthrottle</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">uui</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # admin override until period reset</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bandwidth-node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> reapply</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                # force re-apply of all tc rules</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bandwidth-node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> doctor</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # health diagnostics</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">journalctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bandwidth-node.service</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span></span></code></pre></div><h3 id="uninstall" tabindex="-1">Uninstall <a class="header-anchor" href="#uninstall" aria-label="Permalink to &quot;Uninstall&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> uninstall.sh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                # full removal</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> uninstall.sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --keep-config</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # keep /etc/bandwidth-node</span></span></code></pre></div><p>Stops and disables the service, sweeps the agent&#39;s tc rules from all veths, and removes the binaries, socket, config, state, and logs.</p><div class="warning custom-block"><p class="custom-block-title">DON&#39;T MIX MANUAL tc RULES WITH THE AGENT</p><p>The agent deletes and rebuilds the root and ingress qdiscs on managed veths on every apply. Hand-added qdiscs on those interfaces will be silently replaced — and hand-added rules may confuse <code>VerifyAll</code>/repair. If you need custom shaping, do it on a different interface layer (e.g. the Docker bridge), not on the container veths.</p></div><hr><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><ul><li><strong><a href="./overview">Architecture Overview →</a></strong> — how the agent&#39;s counters reach the panel&#39;s charts.</li><li><strong><a href="./../user-guide/api">API Reference →</a></strong> — the endpoints behind limits pushes and unthrottle calls.</li><li><strong><a href="./../user-guide/admin-panel">Admin Panel Guide →</a></strong> — driving enforcement from the panel UI.</li></ul>`,50)])])}const g=s(n,[["render",r]]);export{k as __pageData,g as default};
