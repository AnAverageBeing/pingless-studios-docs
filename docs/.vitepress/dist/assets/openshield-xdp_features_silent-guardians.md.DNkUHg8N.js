import{_ as t,o as a,c as s,a2 as r}from"./chunks/framework.CZqXuAiL.js";const u=JSON.parse('{"title":"Silent Guardians","description":"","frontmatter":{},"headers":[],"relativePath":"openshield-xdp/features/silent-guardians.md","filePath":"openshield-xdp/features/silent-guardians.md","lastUpdated":1785959583000}'),o={name:"openshield-xdp/features/silent-guardians.md"};function n(i,e,l,d,c,h){return a(),s("div",null,[...e[0]||(e[0]=[r('<h1 id="silent-guardians" tabindex="-1">Silent Guardians <a class="header-anchor" href="#silent-guardians" aria-label="Permalink to &quot;Silent Guardians&quot;">​</a></h1><p>The best protections are the ones you never configured and never notice. This page documents every invisible guard in OpenShield-XDP — the things that keep <em>your</em> traffic safe while the firewall hunts attackers. Most exist because a real user hit the failure mode first; each is now permanent.</p><hr><h2 id="your-sftp-uploads-never-get-you-banned" tabindex="-1">Your SFTP uploads never get you banned <a class="header-anchor" href="#your-sftp-uploads-never-get-you-banned" aria-label="Permalink to &quot;Your SFTP uploads never get you banned&quot;">​</a></h2><p><strong>The problem:</strong> a client uploading a 2 GB world file over SFTP looks <em>exactly</em> like a TCP flood to any rate-based firewall — thousands of packets per second from one IP, sustained. In early versions, uploaders got banned mid-transfer. Every time.</p><p><strong>The guard — established-connection exemption (A2):</strong> OpenShield doesn&#39;t trust rate; it trusts <em>proof</em>. When a source completes a real TCP handshake and then sends a <strong>data segment</strong> — actual payload — the kernel marks it <code>established</code>. From that moment the source skips PPS/BPS/TCP rate scoring entirely.</p><p>The details are what make it bulletproof:</p><ul><li>The mark requires a <strong>payload-carrying</strong> segment after a recent SYN. A blind ACK flood — even one smart enough to send a SYN first — carries no payload and never earns the mark.</li><li>Marking works <strong>even on presets where connection-tracking enforcement is off</strong> (Hosting, Performance, CDN) — observation and enforcement are deliberately decoupled, so bulk transfers on those profiles are protected too.</li><li>SYN-rate, connection-rate and UDP scoring stay fully active for established sources — an attacker can&#39;t ride a real connection to flood harder.</li><li>The mark lives in the per-IP LRU entry and expires naturally with it.</li></ul><p>File uploads, backups, rsync, database replication, big API responses — all protected by the same proof. Knob: <code>static.ct_established_exempt</code> (default: on).</p><h2 id="your-own-downloads-never-fake-an-attack" tabindex="-1">Your own downloads never fake an attack <a class="header-anchor" href="#your-own-downloads-never-fake-an-attack" aria-label="Permalink to &quot;Your own downloads never fake an attack&quot;">​</a></h2><p><strong>The problem:</strong> the server itself downloads things — GeoIP database updates (~30 MB), blocklist feeds, apt packages, backups. That&#39;s a multi-thousand-PPS inbound burst from a CDN, arriving the moment the loader starts. It crossed the attack floors and got declared a <strong>TCP_FLOOD</strong> — which then tightened every threshold server-wide against your real users.</p><p><strong>The guard — outbound-response exemption:</strong> the XDP program recognizes replies to connections the server itself initiated (non-SYN TCP to an ephemeral port; UDP only from well-known source ports). Such traffic:</p><ul><li>skips per-source rate limiting and suspicion scoring (so the CDN never gets banned), and</li><li>is counted in separate <code>outresp_*</code> counters that are <strong>subtracted from the rates the attack classifier and baseline learner see</strong> (since v2.3.1) — your own traffic can never declare an attack or inflate the baseline.</li></ul><p>The totals still count everything, so the dashboard shows real throughput. Only detection is filtered. SYN floods aimed at high ports are still caught — bare SYNs are never treated as responses.</p><h2 id="your-admin-ip-whitelists-itself" tabindex="-1">Your admin IP whitelists itself <a class="header-anchor" href="#your-admin-ip-whitelists-itself" aria-label="Permalink to &quot;Your admin IP whitelists itself&quot;">​</a></h2><p>Every time the loader starts in an SSH session, OpenShield reads <code>SSH_CONNECTION</code>, takes <em>your</em> source IP, and whitelists it — live in the map <strong>and</strong> persisted to the config file. Persisted means it survives reboots: after a boot autostart (<code>openshield load -always</code>), the whitelist is re-applied from disk before the first packet is judged. Even if you remove it by hand, the next interactive load re-adds it.</p><p>The firewall can never lock out its own operator.</p><h2 id="players-never-disconnect-mid-attack" tabindex="-1">Players never disconnect mid-attack <a class="header-anchor" href="#players-never-disconnect-mid-attack" aria-label="Permalink to &quot;Players never disconnect mid-attack&quot;">​</a></h2><p><strong>The problem:</strong> the per-port attack cap is the best weapon against rotating floods — but a naive cap on a Minecraft port would throttle <em>everyone</em> on it, kicking real players along with the bots.</p><p><strong>The guard — player protection:</strong> two classes of sources are exempt from the port cap:</p><ol><li><strong>Established sources</strong> — they proved a real TCP session (Java Edition keeps playing untouched).</li><li><strong>Pre-attack sources</strong> — anyone whose first packet arrived <em>before</em> the attack started (Bedrock/UDP players, long-standing clients). Flood bots by definition appear <em>after</em>.</li></ol><p>A legit <em>new</em> player joining mid-attack shares the capped pool — a degraded join, not a disconnect. Attackers get the drop; your community doesn&#39;t.</p><h2 id="the-baseline-can-t-be-poisoned" tabindex="-1">The baseline can&#39;t be poisoned <a class="header-anchor" href="#the-baseline-can-t-be-poisoned" aria-label="Permalink to &quot;The baseline can&#39;t be poisoned&quot;">​</a></h2><p>Dynamic detection compares live traffic against a learned baseline. Attackers know this — the &quot;slow climb&quot; bypass gradually raises traffic so the baseline inflates and the spike threshold chases it upward, rendering the flood invisible.</p><p>Three guards make that impossible:</p><ul><li><strong>Learning freezes during attacks</strong> — attack traffic never enters the EMA.</li><li><strong>Above-trigger ceiling</strong> — traffic over the effective trigger is <em>never</em> learned, declared attack or not. Organic growth below the trigger still is.</li><li><strong>Behavior engine freeze</strong> — the clustering engine also stops learning while an attack is active, and poisoned state is never persisted.</li></ul><h2 id="no-false-attack-storms" tabindex="-1">No false attack storms <a class="header-anchor" href="#no-false-attack-storms" aria-label="Permalink to &quot;No false attack storms&quot;">​</a></h2><p>Declaring &quot;under attack&quot; tightens every threshold — a false positive hurts. The classifier is wrapped in hygiene:</p><ul><li><strong>Hysteresis</strong> — the spike must persist for N consecutive intervals (<code>attack_trigger_time</code>, default 3) before declaration.</li><li><strong>Recovery band</strong> — attacks end at spike × factor, not at the baseline, so they don&#39;t linger for hours on busy servers.</li><li><strong>Re-trigger cooldown</strong> — within 10 s of an attack ending, re-declaring requires a clearly renewed spike (≥2×).</li><li><strong>Anti-flap re-classification</strong> — type changes need two consecutive sightings, 15 s apart.</li><li><strong>Absolute timeout</strong> — <code>attack_max_duration</code> ends any stuck state.</li><li><strong>Absolute floors</strong> — <code>attack_min_pps/bps</code> mean a collapsed baseline can never silently disable detection.</li><li><strong>Startup warmup</strong> — <code>attack_warmup_sec</code> (default 20) suppresses <em>declaration</em> right after loader start, when mass reconnects and catch-up bursts look like spikes. Per-IP mitigation runs unaffected.</li></ul><h2 id="lists-clean-up-after-themselves" tabindex="-1">Lists clean up after themselves <a class="header-anchor" href="#lists-clean-up-after-themselves" aria-label="Permalink to &quot;Lists clean up after themselves&quot;">​</a></h2><ul><li><strong>Unban-before-whitelist</strong> — whitelisting a banned IP removes the ban first; the two lists can&#39;t fight.</li><li><strong>Moves</strong> — blacklist→whitelist (and back) is one action, not two error-prone steps.</li><li><strong>Never-block (whitelabel)</strong> — auto-fetch feeds can never block your chosen IPs/subnets, without whitelisting them from the firewall itself.</li><li><strong>Import hygiene</strong> — file imports validate every line, strip comments, skip invalid entries, and deduplicate.</li><li><strong>Bulk-ban safety filter</strong> — mass-blacklisting an attack&#39;s IP list skips whitelisted and established sources automatically.</li><li><strong>Feed failure tolerance</strong> — fetched entries expire at 2× the fetch interval, so a broken or hijacked feed never perma-blocks.</li></ul><h2 id="the-firewall-protects-itself" tabindex="-1">The firewall protects itself <a class="header-anchor" href="#the-firewall-protects-itself" aria-label="Permalink to &quot;The firewall protects itself&quot;">​</a></h2><p>Under a mega-flood, a firewall that logs every drop or emits an event per packet dies of its own observability:</p><ul><li><strong>Log flood pause</strong> — new sources over 500/s or attack PPS over 10k pauses file logging (<code>LOG PAUSED</code> in the TUI), resuming automatically.</li><li><strong>BPF event rate limiting</strong> — per-second event caps in the kernel; the ring buffer never blocks the datapath.</li><li><strong>Alert queue</strong> — webhook dispatch drops rather than blocking the firewall; warnings are coalesced to one per 30 s; ban-batch events are packed into single embeds every 5 s to stay under Discord rate limits.</li></ul><h2 id="crash-proof-state" tabindex="-1">Crash-proof state <a class="header-anchor" href="#crash-proof-state" aria-label="Permalink to &quot;Crash-proof state&quot;">​</a></h2><p>Restarting — or crashing — never loses your security posture:</p><ul><li><strong>Bans</strong> persist to disk on daemonize and restore with their <em>remaining</em> lifetimes; the file is deleted after restore so a crash loop can&#39;t re-apply stale bans.</li><li><strong>Baseline</strong>, <strong>geo blocks</strong>, <strong>ban notes</strong>, and <strong>suppression schedules</strong> all persist and restore.</li><li><strong>Closing the TUI never stops protection</strong> — <code>q</code> hands off to systemd (or a re-exec daemon with a liveness check); bans and baseline carry over.</li><li><strong>Self-repair</strong> — a failed load triggers automatic cleanup and retry; <code>openshield fix</code> handles the rest; stale pins and crashed predecessors are detected and taken over gracefully.</li><li><strong>Boot autostart</strong> — <code>openshield load -always</code> enables the hardened systemd unit: it waits for the network to actually be online, restarts forever without giving up, and re-applies your persisted whitelist.</li></ul><h2 id="fail-open-where-it-matters" tabindex="-1">Fail-open where it matters <a class="header-anchor" href="#fail-open-where-it-matters" aria-label="Permalink to &quot;Fail-open where it matters&quot;">​</a></h2><ul><li><strong>No config loaded = pass all.</strong> If the config map is empty, XDP passes everything — a broken deploy can never blackhole your server.</li><li><strong>Empty MAC whitelist fails open</strong> — enabling MAC-filter whitelist mode with zero entries warns and passes instead of dropping 100% of traffic. ARP is always exempt, so gateway resolution never breaks.</li><li><strong>License grace</strong> — an unreachable license server doesn&#39;t instantly disarm you: cached state plus a configurable grace period keeps protection running.</li></ul><h2 id="pcap-that-survives" tabindex="-1">PCAP that survives <a class="header-anchor" href="#pcap-that-survives" aria-label="Permalink to &quot;PCAP that survives&quot;">​</a></h2><p>The rolling capture (~3 minutes of pre-attack history in three 60-second files) watches its own tcpdump process and respawns it on failure. When an attack is declared, the relevant ring file is harvested into the attack&#39;s forensics bundle automatically.</p><hr><p>These guards are why OpenShield can be aggressive by default: the more certain it is about what <em>legitimate</em> looks like, the harder it can swing at everything else.</p>',43)])])}const g=t(o,[["render",n]]);export{u as __pageData,g as default};
