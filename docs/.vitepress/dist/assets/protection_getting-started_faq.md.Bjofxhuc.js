import{_ as t,o as a,c as o,a2 as s}from"./chunks/framework.CZqXuAiL.js";const p=JSON.parse('{"title":"FAQ","description":"Common questions about Protection Plus — DDoS scope, false positives, Tor evasion, resource usage, watchdog recovery, and excluding paths or containers.","frontmatter":{"title":"FAQ","description":"Common questions about Protection Plus — DDoS scope, false positives, Tor evasion, resource usage, watchdog recovery, and excluding paths or containers."},"headers":[],"relativePath":"protection/getting-started/faq.md","filePath":"protection/getting-started/faq.md","lastUpdated":1785339549000}'),i={name:"protection/getting-started/faq.md"};function n(r,e,d,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="faq" tabindex="-1">FAQ <a class="header-anchor" href="#faq" aria-label="Permalink to &quot;FAQ&quot;">​</a></h1><h2 id="does-it-stop-inbound-ddos-attacks-against-my-node" tabindex="-1">Does it stop inbound DDoS attacks against my node? <a class="header-anchor" href="#does-it-stop-inbound-ddos-attacks-against-my-node" aria-label="Permalink to &quot;Does it stop inbound DDoS attacks against my node?&quot;">​</a></h2><p><strong>No.</strong> Protection Plus is <em>egress</em> protection: it stops your <strong>customers</strong> from attacking other people — outbound floods, port scans, miners, Tor exits. Mitigating inbound attacks aimed at your nodes is the job of your XDP firewall / upstream layer (see the XDP.Network firewall). The two are complementary: one keeps abuse out, the other keeps your IPs clean.</p><h2 id="will-it-flag-my-game-servers-as-miners-or-floods" tabindex="-1">Will it flag my game servers as miners or floods? <a class="header-anchor" href="#will-it-flag-my-game-servers-as-miners-or-floods" aria-label="Permalink to &quot;Will it flag my game servers as miners or floods?&quot;">​</a></h2><p>Not by design — and if it does, you have the knobs to fix it. Game-server processes (<code>java</code>, <code>bedrock_server</code>, <code>srcds</code>, <code>RustDedicated</code>, <code>fivem</code>, <code>valheim</code>, …) are on the built-in <code>whitelist_processes</code> lists, so they are <strong>exempt from the CPU and connection-flood heuristics</strong> that catch unknown miners and floods. Note the nuance: <em>signature</em> matching still applies to them, so a miner actually named <code>xmrig</code> inside a game container is still caught. Thresholds are also tuned around game workloads — miners sustain high CPU flat, game servers spike and dip (<code>cpu_threshold: 85</code> / <code>sustained_seconds: 45</code>); game servers rarely exceed 10–20k pps against the 60k <code>pps_threshold</code>. And everything ships in <code>dry_run: true</code>, so you see what it <em>would</em> flag before it ever acts.</p><h2 id="a-customer-runs-tor-on-a-custom-port-is-it-caught" tabindex="-1">A customer runs Tor on a custom port. Is it caught? <a class="header-anchor" href="#a-customer-runs-tor-on-a-custom-port-is-it-caught" aria-label="Permalink to &quot;A customer runs Tor on a custom port. Is it caught?&quot;">​</a></h2><p>Yes. The abuse detector doesn&#39;t rely on port numbers alone — it matches the <code>tor</code> binary itself and Tor&#39;s command-line flags (<code>--orport</code>, <code>--socksport</code>, …), which fire regardless of which port the relay binds. Listening sockets on the <em>well-known</em> Tor/SOCKS/proxy ports (9001/9050/1080/…) are a separate, additional signal. If your tenants favour a specific custom port, add it to <code>detectors.abuse.abusive_ports</code>.</p><h2 id="how-much-cpu-ram-does-it-use" tabindex="-1">How much CPU/RAM does it use? <a class="header-anchor" href="#how-much-cpu-ram-does-it-use" aria-label="Permalink to &quot;How much CPU/RAM does it use?&quot;">​</a></h2><p>Very little. One shared <code>/proc</code> snapshot per scan tick (default every 5s) feeds every detector, and socket→PID resolution is parallelised with a hard 1.5s time budget. Measured on live nodes: <strong>~13 MB RSS and 1–3% CPU</strong> at the default interval. The systemd unit additionally caps it at <code>MemoryMax=256M</code> and <code>CPUQuota=50%</code> as guardrails.</p><h2 id="what-happens-if-the-daemon-hangs-or-runs-out-of-memory" tabindex="-1">What happens if the daemon hangs or runs out of memory? <a class="header-anchor" href="#what-happens-if-the-daemon-hangs-or-runs-out-of-memory" aria-label="Permalink to &quot;What happens if the daemon hangs or runs out of memory?&quot;">​</a></h2><p>The systemd unit is built for exactly this. The daemon pets systemd after <strong>every scan tick</strong>; if the detection loop hangs for longer than <code>WatchdogSec=60s</code>, systemd kills and restarts the service (<code>Restart=on-failure</code>). If it ever exceeds the <code>MemoryMax=256M</code> cap it is OOM-killed and restarted the same way. A noisy neighbour can never stall the loop either — socket resolution is time-budgeted and unresolved connections are simply skipped that tick. Optional <code>limits:</code> in the config add per-detector timeouts and alert rate caps for pathological nodes.</p><h2 id="how-do-i-exclude-a-path-or-a-container-from-everything" tabindex="-1">How do I exclude a path or a container from everything? <a class="header-anchor" href="#how-do-i-exclude-a-path-or-a-container-from-everything" aria-label="Permalink to &quot;How do I exclude a path or a container from everything?&quot;">​</a></h2><p>Use the <code>whitelist:</code> section of the config:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">whitelist</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  paths</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/srv/trusted-builds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># prefix-matched: exempts everything beneath</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  containers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">my-admin-container</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># full ID, short ID, or name</span></span></code></pre></div><p>Whitelisted paths are <strong>never scanned or flagged</strong>, even if they also fall under a scan/watch path; whitelisted containers are never flagged, killed, or suspended. Matching events are dropped before the rules engine ever sees them. For finer control, per-detector <code>whitelist_processes</code> lists exempt processes from specific heuristics.</p><h2 id="does-it-run-inside-my-game-server-containers" tabindex="-1">Does it run inside my game/server containers? <a class="header-anchor" href="#does-it-run-inside-my-game-server-containers" aria-label="Permalink to &quot;Does it run inside my game/server containers?&quot;">​</a></h2><p>No. Protection Plus runs on the <strong>host</strong> and watches every container from the outside via <code>/proc</code> and the Docker socket. Tenants inside a container can&#39;t see it, disable it, or evade it — and it reads each container&#39;s network namespace directly, so connections made <em>from inside</em> a container are still seen and attributed. Verify with <code>sudo protection debug-conns</code>.</p><h2 id="will-it-nuke-a-server-on-a-false-positive" tabindex="-1">Will it nuke a server on a false positive? <a class="header-anchor" href="#will-it-nuke-a-server-on-a-false-positive" aria-label="Permalink to &quot;Will it nuke a server on a false positive?&quot;">​</a></h2><p>Not unless you let it. It ships in <strong><code>dry_run: true</code></strong> — it only detects and alerts until you explicitly arm enforcement. Every threshold and signature list is tunable, and the smart <code>neutralize</code> action refuses to touch PID ≤ 1. Follow the <a href="./quick-start">Quick Start</a> dry-run workflow.</p><h2 id="does-it-need-the-docker-sdk-libpcap-yara-or-a-database" tabindex="-1">Does it need the Docker SDK, libpcap, YARA, or a database? <a class="header-anchor" href="#does-it-need-the-docker-sdk-libpcap-yara-or-a-database" aria-label="Permalink to &quot;Does it need the Docker SDK, libpcap, YARA, or a database?&quot;">​</a></h2><p>No. It&#39;s a single static binary (zero cgo) that reads <code>/proc</code> and talks plain HTTP to <code>/var/run/docker.sock</code>. The YARA and trivy CLIs are <strong>optional</strong> extras — install them only if you want YARA file scanning or image vulnerability scanning; without them those checks are silent no-ops. The on-access hash blocklist just needs one <code>protection rules update</code>.</p><h2 id="i-run-a-plain-vps-not-pterodactyl-is-it-useful" tabindex="-1">I run a plain VPS, not Pterodactyl. Is it useful? <a class="header-anchor" href="#i-run-a-plain-vps-not-pterodactyl-is-it-useful" aria-label="Permalink to &quot;I run a plain VPS, not Pterodactyl. Is it useful?&quot;">​</a></h2><p>Yes. Set <code>mode: server</code> (or <code>both</code>). The <code>neutralize</code> action kills the offending <strong>process</strong> when there&#39;s no container, so host miners, scanners, reverse shells and bombs are all handled. Just leave the Pterodactyl action disabled.</p><h2 id="why-does-it-need-root" tabindex="-1">Why does it need root? <a class="header-anchor" href="#why-does-it-need-root" aria-label="Permalink to &quot;Why does it need root?&quot;">​</a></h2><p>Reading other users&#39; processes, mapping sockets to owning PIDs, reading per-process disk I/O (for the zip-bomb hot-trigger), and entering container network namespaces all require root (or <code>CAP_SYS_PTRACE</code> + <code>CAP_DAC_READ_SEARCH</code> + <code>CAP_KILL</code>). The bundled systemd unit runs as root with hardening applied — read-only filesystem, pinned capabilities, resource caps.</p><h2 id="can-i-disable-a-specific-detector" tabindex="-1">Can I disable a specific detector? <a class="header-anchor" href="#can-i-disable-a-specific-detector" aria-label="Permalink to &quot;Can I disable a specific detector?&quot;">​</a></h2><p>Yes — set its <code>enabled: false</code> in the config, or delete its block entirely. Each of the ten detectors (<code>miner</code>, <code>portscan</code>, <code>ddos</code>, <code>zipbomb</code>, <code>exploit</code>, <code>abuse</code>, <code>onaccess</code>, <code>fim</code>, <code>yara</code>, <code>trivy</code>) is independent.</p><h2 id="how-do-i-change-what-happens-when-a-threat-is-found" tabindex="-1">How do I change what happens when a threat is found? <a class="header-anchor" href="#how-do-i-change-what-happens-when-a-threat-is-found" aria-label="Permalink to &quot;How do I change what happens when a threat is found?&quot;">​</a></h2><p>Edit the <a href="./../user-guide/actions-rules"><code>rules</code></a> section. Rules map a threat category + minimum severity to a list of actions (<code>alert</code>, <code>neutralize</code>, <code>kill_container</code>, <code>suspend_server</code>, <code>quarantine_file</code>, …).</p><h2 id="how-do-i-detect-a-brand-new-miner-that-isn-t-in-the-signature-list" tabindex="-1">How do I detect a brand-new miner that isn&#39;t in the signature list? <a class="header-anchor" href="#how-do-i-detect-a-brand-new-miner-that-isn-t-in-the-signature-list" aria-label="Permalink to &quot;How do I detect a brand-new miner that isn&#39;t in the signature list?&quot;">​</a></h2><p>The miner detector also flags <strong>sustained high CPU</strong> (configurable <code>cpu_threshold</code> / <code>sustained_seconds</code>) and <strong>masked binaries</strong> (a deleted-on-disk executable burning CPU), so unknown miners are still caught. Signature + sustained CPU together escalates to critical.</p><h2 id="where-do-quarantined-files-go" tabindex="-1">Where do quarantined files go? <a class="header-anchor" href="#where-do-quarantined-files-go" aria-label="Permalink to &quot;Where do quarantined files go?&quot;">​</a></h2><p>To <code>actions.file.quarantine_dir</code> (default <code>/var/lib/protection/quarantine</code>), moved and <code>chmod 000</code> so they can&#39;t be executed — evidence is preserved rather than deleted.</p>`,33)])])}const m=t(i,[["render",n]]);export{p as __pageData,m as default};
